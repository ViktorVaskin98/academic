# Авито · DS ML/NLP — разбор слива

Источник: `Авито МЛ гайд.pdf` (Поступашки, 19 стр.). 26 вопросов, 5 разделов, грейды DS2–DS4.

Этот файл — не копия PDF. Для каждого вопроса здесь:
- **Q** — что спрашивают (в сжатой формулировке);
- **Ядро ответа** — минимум, который должен вылетать за 30–60 сек;
- **⚠️ В сливе** — где ответ из PDF неточен или содержит ошибку (такие ответы опаснее, чем их отсутствие);
- **🎯 Follow-up** — куда интервьюер, скорее всего, потянет дальше;
- **📚 Где лежит** — билеты из уже готовых подготовок (`ml_sobes` = Б-xx, `deeeeeeep` = DL-xx).

---

## 0. Что слив говорит о собесе

| Раздел | Задач | DS2 | DS3 | DS4 |
|---|---|---|---|---|
| Основы ML | 8 | 3 | 3 | 2 |
| Ансамбли | 5 | 2 | 2 | 1 |
| Computer Vision | 3 | 1 | 1 | 1 |
| NLP | 8 | 2 | 6 | 0 |
| Продвинутые (DL) | 2 | 1 | 1 | 0 |
| **Итого** | **26** | **9** | **13** | **4** |

Выводы:
1. **Половина слива — классический ML** (13 из 26). Это пересекается с подготовкой к Яндексу почти один в один.
2. **NLP в сливе — эпоха BERT (2018–2020)**: эмбеддинги, трансформер, BERT, QA. Ни слова про LLM, токенизацию BPE, LoRA, RAG, LLM-as-judge. Для NLP-позиции в 2026 это почти наверняка **не весь скоуп** — см. раздел 7.
3. **DS4-вопросы — это кейсы про прод Авито**, а не теория: модерация, пороги, ручная проверка, скорость инференса. Сквозной домен — **модерация объявлений и антифрод** (контакты в тексте, дубли аккаунтов). Под NLP-команду это самый вероятный продуктовый контекст.
4. Формат ответа ценится «объясни идею», а не «назови термин» — это прямо сказано в сливе и видно по follow-up.

---

## 1. Основы ML (8)

### #1 Регуляризация · DS2 · `regularization`
**Q.** Что такое регуляризация, какие виды для линейных моделей. Почему штраф на норму весов борется с переобучением.

**Ядро ответа.**
- Добавка к функционалу: $Q(w) + \lambda R(w)$. L2: $\|w\|_2^2$ (Ridge), L1: $\|w\|_1$ (Lasso), Elastic Net: $\alpha\|w\|_1 + \beta\|w\|_2^2$.
- Почему помогает: переобученная линейная модель — это огромные веса, которые компенсируют друг друга (типичная ситуация при мультиколлинеарности). Малое изменение признака → огромное изменение ответа. Штраф ограничивает «разрешённую сложность» модели.
- Байесовский взгляд: L2 = MAP с гауссовым приором, L1 = MAP с лапласовым.

**⚠️ В сливе.** Аргумент «переобучение = большие веса» дан без связи с мультиколлинеарностью и спектром $X^\top X$ — это самое сильное объяснение, его стоит дать.

**🎯 Follow-up.** Что Ridge делает с собственными числами $X^\top X$. Байесовская интерпретация. Нужно ли масштабировать признаки перед регуляризацией (да — иначе штраф неравномерный).

**📚** Б-15, Б-16, Б-17, Б-19, Б-20 · DL-5, DL-17

---

### #2 Линейная регрессия · DS2
**Q.** Аналитическое решение МНК. Почему на практике не считают по формуле. Сложность обращения матрицы. Как тогда решают.

**Ядро ответа.**
- $\frac{1}{\ell}\|Xw - y\|_2^2 \to \min_w \;\Rightarrow\; w^* = (X^\top X)^{-1}X^\top y$.
- Почему не по формуле: (1) обращение $d\times d$ — $O(d^3)$, плюс сборка $X^\top X$ за $O(\ell d^2)$; (2) $X^\top X$ может быть вырождена / плохо обусловлена (мультиколлинеарность, $d > \ell$) → неустойчивость. Лечится регуляризацией: $(X^\top X + \lambda I)^{-1}$.
- Сложность: наивно $O(n^3)$, Штрассен $O(n^{2.81})$, теоретически ещё быстрее, но с огромными константами.
- Как решают: GD / SGD; в пакетах — QR или SVD, а не явное обращение.

**⚠️ В сливе.** Опечатки в формулах: норма без квадрата $|Xw-y|$ и $w = (XX^\top)^{-1}X^\top y$ — **правильно $(X^\top X)^{-1}X^\top y$**. На доске такая ошибка стоит дорого.

**🎯 Follow-up.** Вывести формулу (градиент, приравнять нулю). Геометрия — проекция $y$ на колонки $X$. Число обусловленности и почему масштабирование ускоряет GD. Чем SGD хорош при большом $\ell$.

**📚** Б-2, Б-3, Б-5, Б-6, Б-29

---

### #3 Почему L1 разреживает, а L2 нет · DS3 · `regularization`
**Q.** Объяснить разреживание L1. Нужно ли регуляризовать $w_0$.

**Ядро ответа.**
- **Геометрия:** минимизируем $Q(w)$ при $R(w)\le t$. Линии уровня $Q$ — эллипсы, область L1 — ромб с углами на осях. Касание эллипса с ромбом с большой вероятностью попадает в угол → часть координат ровно 0. У круга (L2) углов нет → касание в общей точке.
- **Через градиент:** $|\partial R/\partial w_i|$ для L2 это $2\lambda w_i \to 0$ при $w_i \to 0$ — штраф «выдыхается» и не дотягивает до нуля. Для L1 он константа $\lambda$ — давит с одной силой до самого нуля (точнее: субградиент в нуле $[-\lambda,\lambda]$, и если $|\partial Q/\partial w_i| < \lambda$, ноль — оптимум).
- **$w_0$ не регуляризуем:** штраф на сдвиг тянет гиперплоскость к началу координат, хотя данные могут лежать где угодно; качество почти всегда хуже.

**⚠️ В сливе.** Нормально, но субградиентное условие оптимальности в нуле не сформулировано — а это самый строгий аргумент.

**🎯 Follow-up.** Решение Lasso в одномерном случае (soft thresholding). Когда Elastic Net лучше Lasso (группы коррелированных признаков — Lasso берёт один случайный).

**📚** Б-17, Б-18, Б-19 (есть гифка `viz/l1_l2_geometry_bilet15.gif` в ветке `ml`)

---

### #4 Precision / Recall на кейсе модерации · DS4 · `metrics` ⭐
**Q.** Модель ищет контакты в описании объявления. Нужно два порога: автоотклонение и отправка в ручную модерацию (ресурс ограничен). Что важнее для каждого порога — precision или recall?

**Ядро ответа.**
- **Автоотклонение → precision.** Ложное автоотклонение бьёт по честному пользователю, и исправить это некому: жалобы в поддержку, отток, негатив. Порог выбираем так, чтобы precision был ≥ заданного бизнесом уровня (например, 99%).
- **Ручная модерация → recall при ограничении по объёму.** Люди исправят ложные срабатывания, поэтому максимизируем полноту, но при условии, что поток ≤ ёмкости модераторов. Порог = такой, что в зону [порог_ручной, порог_авто) попадает ровно столько, сколько успеют разобрать.
- Итог — три зоны по скору: авто-reject / в очередь / пропустить.

**🎯 Follow-up.** Как выбирать порог на практике (по валидации, с учётом дрейфа доли нарушителей). Что если ёмкость модераторов меняется по дням. Как мерить качество ручной зоны (precision@k). Как модерационные решения превращаются в новую разметку и какие смещения это создаёт (selection bias — видим метки только того, что отправили).

**📚** Б-32, Б-37, Б-74 · это ядро будущего NLP-кейса, стоит дожать до MLSD-формата

---

### #5 AUC-ROC, MAE vs MSE, базовые метрики · DS2 · `auc_roc`
**Q.** Что такое AUC-ROC. Регрессия с выбросами — MAE или MSE. Precision, recall, accuracy.

**Ядро ответа.**
- AUC = площадь под ROC = **вероятность того, что случайный положительный получит скор выше случайного отрицательного** (доля верно упорядоченных пар).
- С выбросами — MAE: MSE штрафует квадратом, выброс тянет всю модель. Ещё сильнее: MSE оценивает **среднее**, MAE — **медиану** условного распределения. Минус MAE — нет производной в нуле, градиент не уменьшается у оптимума. Компромисс — Huber.
- $\text{Precision} = \frac{TP}{TP+FP}$, $\text{Recall} = \frac{TP}{TP+FN}$, $\text{Accuracy} = \frac{TP+TN}{\text{всё}}$ — и почему accuracy врёт на дисбалансе.

**🎯 Follow-up.** Как строится ROC-кривая по точкам. Что такое AUC = 0.5 и AUC < 0.5. Почему MSE ↔ гауссов шум, MAE ↔ лапласов (ММП).

**📚** Б-7, Б-20, Б-31, Б-32, Б-34, Б-35

---

### #6 AUC при дублировании объектов · DS3 · `auc_roc`
**Q.** AUC = 0.92. Каждый положительный объект продублировали 7 раз, каждый отрицательный — 4. Что станет с AUC?

**Ядро ответа.** Не изменится. $\text{AUC} = \frac{1}{N_+ N_-}\sum_{i\in +}\sum_{j\in -}[s_i > s_j]$. После дублирования каждая исходная пара превращается в $7\cdot 4 = 28$ одинаковых пар; числитель и знаменатель умножаются на 28. Пары дубль-с-дублём одного класса в AUC не входят.

**🎯 Follow-up.** А если продублировать только часть положительных (изменится — это перевзвешивание). Изменится ли PR-AUC при таком дублировании (да — precision зависит от соотношения классов).

**📚** Б-35

---

### #7 AUC > 0.99, а в топ-1000 ни одного положительного · DS4 · `auc_roc` ⭐
**Q.** Поиск контактов, AUC > 0.99. В ручную модерацию уходят 1000 объектов с максимальным скором из 1 млн в день. Может ли среди них не быть ни одного положительного?

**Ядро ответа.** Да. Пример: 1 тыс. положительных, 999 тыс. отрицательных. Отсортируем по скору по возрастанию: 990 тыс. отрицательных → 1 тыс. положительных → 9 тыс. отрицательных. Положительный выше отрицательного в $\frac{1000\cdot 990\,000}{1000\cdot 999\,000} \approx 0.991$ пар → AUC > 0.99, а топ-1000 целиком отрицательный.
**Мораль:** ROC-AUC — глобальная метрика ранжирования, при сильном дисбалансе её почти целиком определяет масса «лёгких» отрицательных. Про верх списка она ничего не гарантирует.

**⚠️ В сливе.** Нет вывода «что мерить вместо» — а это и есть смысл вопроса.

**🎯 Follow-up.** Какую метрику брать: **precision@k** (k = ёмкость модерации), PR-AUC, recall при фиксированном потоке. Почему PR-кривая честнее при дисбалансе.

**📚** Б-35, Б-36

---

### #8 Модификации GD и momentum · DS3
**Q.** Какие модификации GD знаешь. Что такое SGD + momentum. Пример линий уровня, где momentum сильно быстрее.

**Ядро ответа.**
- SGD, momentum, Nesterov, Adagrad, RMSProp, Adam, AdamW.
- Momentum: $v_t = \beta v_{t-1} + \nabla Q(w_t)$, $w_{t+1} = w_t - \eta v_t$ — экспоненциальное среднее градиентов.
- **Вытянутые эллипсы** (плохая обусловленность): градиент перпендикулярен линиям уровня → GD скачет поперёк «оврага» и медленно ползёт вдоль. В momentum поперечные компоненты с чередующимся знаком гасят друг друга, продольные — накапливаются.

**⚠️ В сливе.** «AdaGrad (RMSProp)» — это **два разных метода**: Adagrad копит сумму квадратов градиентов (шаг только убывает, может «умереть»), RMSProp заменяет сумму на экспоненциальное среднее — это исправление Adagrad. Если сказать «AdaGrad, он же RMSProp», поймают.

**🎯 Follow-up.** Линия Adagrad → RMSProp → Adam и что чинит каждый шаг. Bias correction в Adam. Чем AdamW отличается от Adam + L2 (развязанное затухание — для трансформеров это стандарт).

**📚** Б-24–Б-27 · DL-29, DL-31–DL-33, DL-35 (есть гифка `viz/momentum_vs_gd_bilet20.gif`)

---

## 2. Ансамбли (5)

### #9 Бэггинг · DS2 · `bagging`
**Q.** Что такое бэггинг. Что будет при бэггинге линейных моделей.

**Ядро ответа.** Bootstrap aggregation: $B$ бутстреп-выборок (с возвращением, ~63.2% уникальных объектов), на каждой — модель, ответы усредняем. Бэггинг линейных моделей — снова линейная модель (среднее линейных функций линейно), выигрыш по дисперсии минимален, потому что линейная модель и так стабильна. Бэггинг имеет смысл над **нестабильными** моделями с низким bias и высокой variance — глубокими деревьями.

**📚** Б-44, Б-45

---

### #10 Random Forest; бэггинг vs бустинг на 10 000 деревьев · DS3 · `bagging`
**Q.** Как устроен RF. Как бэггинг влияет на bias и variance. RF и GBDT остановили по early stopping, потом добавили ещё по 10 000 деревьев — что будет с качеством на валидации?

**Ядро ответа.**
- RF = бэггинг деревьев + **случайное подмножество признаков в каждом узле** (не одно на всё дерево!). Если сэмплить признаки на всё дерево, деревья без сильных признаков получатся мусорными и испортят среднее.
- Bias почти не меняется (≈ bias одного дерева), variance падает. Для $B$ моделей с дисперсией $\sigma^2$ и попарной корреляцией $\rho$: $\text{Var} = \rho\sigma^2 + \frac{1-\rho}{B}\sigma^2$. Рандом признаков нужен ровно для того, чтобы уменьшить $\rho$.
- +10 000 деревьев: **RF — качество выходит на плато** (первое слагаемое не зависит от $B$), **GBDT — переобучается**, каждое дерево подгоняет остатки всё сильнее.

**🎯 Follow-up.** Откуда 63.2% ($1 - 1/e$). OOB-оценка. Почему в RF деревья глубокие, а в бустинге мелкие.

**📚** Б-45, Б-46, Б-47, Б-52 (есть `viz/bagging_variance_bilet55.png`)

---

### #11 Градиентный бустинг, реализации, стекинг · DS2 · `boosting`
**Q.** Что такое GB. Какие реализации знаешь. Стекинг/блендинг.

**Ядро ответа.**
- $a_M(x) = \sum_m \eta\, b_m(x)$; модели учатся последовательно, $b_m$ приближает **антиградиент** $-\partial L(y, a_{m-1}(x))/\partial a$ в точках обучающей выборки. Для MSE это просто остатки.
- XGBoost (второй порядок, регуляризация в функционале, level-wise), LightGBM (leaf-wise рост, гистограммы, GOSS, EFB), CatBoost (oblivious trees, ordered boosting, ordered target statistics для категорий — борьба с утечкой).
- Стекинг: мета-модель на **out-of-fold** предсказаниях базовых. Блендинг: мета-модель на отложенной выборке, проще, но теряет данные.

**🎯 Follow-up.** Вывести шаг бустинга через разложение Тейлора. Почему oblivious trees быстрые на инференсе. Что такое target leakage в CatBoost и как ordered TS это решает.

**📚** Б-49, Б-51, Б-53 (есть `viz/gradient_boosting_steps_bilet57.gif`)

---

### #12 Почему на проде GBDT, а не RF · DS4 · `bagging` `boosting` ⭐
**Q.** Важна скорость ответа — почему часто лучше GBDT, а не RF?

**Ядро ответа.**
- В RF деревья специально глубокие (низкий bias, высокая variance — её гасит усреднение). При большом числе признаков — очень глубокие, инференс медленный.
- В бустинге деревья мелкие (глубина 4–8) — каждое дёшево, а качество сравнимое или лучше.
- Контраргумент «RF параллелится»: GBDT на инференсе параллелится так же (деревья независимы при предсказании). А при онлайн-предсказании **на одном объекте** распараллеливание вообще проигрывает накладным расходам на потоки.

**🎯 Follow-up.** Oblivious trees CatBoost и векторизация. Как ещё ускорить: меньше деревьев, квантование порогов, дистилляция в меньшую модель, батчинг запросов.

**📚** Б-50, Б-51

---

### #13 Гиперпараметры GBDT · DS3
**Q.** Что подбирать в первую очередь. Есть ли в GBDT L1/L2, что там «веса». Как подбирать гиперпараметры.

**Ядро ответа.**
- Первые: глубина / число листьев, learning rate (в паре с числом деревьев + early stopping), min объектов в листе, коэффициенты регуляризации, subsample / colsample.
- L1/L2 есть — «веса» это **значения в листьях**. В XGBoost лист $w_j = -\frac{G_j}{H_j + \lambda}$ — видно, как L2 сжимает листья.
- Подбор: grid (только 2–3 параметра, иначе экспоненциальный взрыв), random search (лучше grid при многих параметрах, из которых важны немногие), байесовская оптимизация (optuna: TPE — моделируем $p(x\mid\text{хорошо})$ и $p(x\mid\text{плохо})$, берём максимум их отношения).

**🎯 Follow-up.** Как работает TPE / гауссовский процесс на пальцах. Почему random search лучше grid (Bergstra & Bengio).

**📚** Б-50 · **дыра:** байесовской оптимизации гиперпараметров нет ни в одном билетнике

---

## 3. Computer Vision (3)

> Для NLP-позиции CV, скорее всего, пройдёт по верхам. Держим на уровне DS2–DS3.

### #14 Свёртка, pooling, Inception, Residual · DS2 · `CV`
**Q.** Что такое свёрточный слой, pooling, Inception module, residual block.

**Ядро ответа.**
- Свёртка: обучаемое ядро $k\times k\times C_{in}$, скользит по входу; $C_{out}$ ядер → $C_{out}$ карт. Разделение весов + локальность → мало параметров, эквивариантность к сдвигу.
- Pooling: max/avg по окну, **без параметров**; уменьшает разрешение → растёт рецептивное поле, появляется инвариантность к малым сдвигам, меньше вычислений.
- Inception: параллельно свёртки 1×1, 3×3, 5×5 и max-pool 3×3, выходы конкатенируются по каналам — сеть сама выбирает масштаб. Чтобы не взорвались вычисления, перед 3×3 и 5×5 (и после pool) ставят **1×1 свёртки для уменьшения числа каналов** (bottleneck).
- Residual: $y = F(x) + x$. Градиент течёт по identity-ветке без затухания; блоку достаточно выучить поправку к тождеству → сеть глубиной 100+ слоёв учится, не деградирует.

**⚠️ В сливе.** (1) «наивный inception приводит к резкому увеличению числа слоёв» — имеется в виду **число каналов** (и FLOPs). (2) «1×1 свёртка схлопывает все слои изображения в один» — неверно: она сжимает $C$ каналов в $C' < C$, не в один. (3) Residual мотивирован через vanishing gradient — но в статье ResNet главный аргумент — **проблема деградации**: глубокая сеть с BatchNorm хуже мелкой даже на *трейне*, это не переобучение и не только затухание градиента.

**📚** DL-63–DL-68, DL-71, DL-72

---

### #15 Как поднять скор на Kaggle без переобучения CNN · DS3 · `CV`
**Q.** CNN обучена. Как, не переобучая её, выжать максимум?

**Ядро ответа.** **Test-time augmentation**: прогоняем несколько аугментированных версий (кропы, флипы, повороты на 90°, если задача инвариантна, изменение яркости/контраста) и усредняем предсказания. Ещё: ансамбль чекпойнтов / SWA, подбор порога под метрику, калибровка.

**🎯 Follow-up.** Для текста: аналог TTA — ансамбль по нескольким сидам / фолдам, усреднение по разным обрезкам длинного текста.

---

### #16 Расскажи статью · DS4 · `CV`
**Q.** Последняя (интересная) прочитанная статья по CV. Как давно.

**Ядро ответа.** Точное название, год, авторы; проблема → идея → как проверяли → что это дало → ограничения. Своими словами, так, чтобы понял человек, который её не читал.

**🎯 Для NLP-позиции спросят статью по NLP.** Готовим 2 статьи: одну «классическую» (Attention Is All You Need / BERT / Sentence-BERT) и одну свежую, релевантную Авито (retrieval / эмбеддинги / LLM для классификации или модерации). **Это отдельная задача в плане.**

---

## 4. NLP (8) — ядро позиции

### #17 Способы построения эмбеддингов слов и текстов · DS2 · `NLP`
**Q.** Какие способы знаешь; кратко идея каждого, как учится word2vec и чем он отличается от FastText.

**Ядро ответа.**
- **BoW / TF-IDF:** разреженный вектор по словарю; TF-IDF гасит частые слова: $\text{tf}(t,d)\cdot\log\frac{N}{\text{df}(t)}$. Не знает синонимов и порядка.
- **word2vec:** skip-gram (по слову предсказываем контекст) или CBOW (по контексту — слово). Softmax по словарю дорог → **negative sampling** (логрег: реальная пара vs $k$ случайных). По сути неявная факторизация сдвинутой PMI-матрицы.
- **GloVe:** явно факторизуем матрицу совместной встречаемости: $w_i^\top \tilde w_j + b_i + \tilde b_j \approx \log X_{ij}$ с весами.
- **FastText:** слово = сумма эмбеддингов его **символьных n-грамм** → есть эмбеддинг для OOV-слов и опечаток, лучше на морфологически богатых языках (русский!).
- **ELMo:** двунаправленная LSTM-языковая модель, эмбеддинг **зависит от контекста**.
- **BERT:** трансформер-энкодер, MLM + NSP, контекстные эмбеддинги подслов.
- Эмбеддинг текста: среднее / TF-IDF-взвешенное среднее словных векторов, doc2vec, пулинг BERT, Sentence-BERT.

**🎯 Follow-up.** Почему негативы сэмплят с частотой $\propto f^{3/4}$. Как word2vec обрабатывает многозначные слова (никак — один вектор). Почему для русского FastText лучше word2vec.

**📚** DL-132, DL-133, DL-136–DL-141, DL-146 · ELMo — **дыра** (DL-142 «другие подходы», проверить)

---

### #18 Encoder vs Decoder в трансформере · DS3 · `transformer`
**Q.** Чем отличаются блоки энкодера и декодера.

**Ядро ответа.**
- **Энкодер:** self-attention (двунаправленный, каждый токен видит всех) → FFN; каждый подслой с residual + LayerNorm.
- **Декодер:** **masked (causal) self-attention** (токен видит только прошлое) → **cross-attention** (Q — из декодера, K и V — из выхода энкодера) → FFN.
- Три семейства: encoder-only (BERT — понимание, классификация, эмбеддинги), decoder-only (GPT — генерация), encoder-decoder (T5, BART — seq2seq: перевод, суммаризация).

**⚠️ В сливе.** Ответа нет — только картинка. Это DS3-вопрос, его надо уметь рассказать полностью с нуля.

**🎯 Follow-up.** Зачем маска на обучении, если на инференсе будущего всё равно нет (teacher forcing — параллельное обучение по всем позициям). Pre-LN vs post-LN. Что такое KV-кэш.

**📚** DL-99, DL-106, DL-107, DL-121

---

### #19 Self-attention · DS3 · `bert` `transformer` ⭐
**Q.** Как работает self-attention. В чём особенность self-attention в трансформере.

**Ядро ответа.**
- Идея: представление токена = взвешенная сумма представлений всех токенов, веса считаются по содержимому. Слово «лук» смотрит на «стрелять» или «жарить» и получает нужный смысл.
- $Q = XW_Q$, $K = XW_K$, $V = XW_V$; $\text{Attention}(Q,K,V) = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}\right)V$.
- $\sqrt{d_k}$: скалярное произведение $d_k$ независимых компонент с единичной дисперсией имеет дисперсию $d_k$ → без деления softmax насыщается, градиенты исчезают.
- Multi-head: $h$ голов в подпространствах размерности $d/h$ — разные типы связей параллельно, стоимость та же.
- Сложность $O(n^2 d)$ по длине.

**⚠️ В сливе.** «Особенность self-attention в трансформере — треугольная маска» — это верно **только для декодера**. В энкодере (BERT!) маски на будущее нет, есть только padding-маска. Для BERT-вопроса это критично: BERT как раз двунаправленный.

**🎯 Follow-up.** Написать формулу и размерности всех матриц. Зачем $\sqrt{d_k}$. Сложность и как её обходят (FlashAttention, sparse / sliding window). Self-attention эквивариантен к перестановкам → нужен positional encoding (мост к #20).

**📚** DL-102, DL-103, DL-105, DL-109

---

### #20 Positional encoding · DS2 · `bert` `transformer`
**Q.** Что такое positional encoding. Изменятся ли эмбеддинги токенов BERT, если переставить слова?

**Ядро ответа.**
- Self-attention не знает порядка → к эмбеддингу токена прибавляем вектор позиции.
- Варианты: синусоидальный (фиксированный, оригинальный трансформер), **обучаемый абсолютный (BERT, до 512 позиций)**, относительный, **RoPE** (поворот Q и K на угол, зависящий от позиции → скалярное произведение зависит только от *разности* позиций; стандарт в LLM), **ALiBi** (линейный штраф по расстоянию, хорошо экстраполирует на длину).
- Перестановка слов в BERT: **да, изменятся** — у слов другие позиционные эмбеддинги, и через attention это изменение расходится по всем токенам, даже по тем, что остались на месте.

**⚠️ В сливе.** Ответ «да, из-за позиционного эмбеддинга» верный, но стоит показать, что понимаешь почему: **без позиционных кодировок self-attention эквивариантен** — выходы просто переставились бы вместе со входами, а вектор каждого слова остался бы прежним. Позиционная кодировка — единственный источник чувствительности к порядку.

**📚** DL-104

---

### #21 Эмбеддинг текста из BERT · DS3 · `bert` ⭐
**Q.** Как из обученного BERT получить эмбеддинг всего текста?

**Ядро ответа.**
- Без дообучения: `[CLS]`-токен, mean pooling по токенам (с учётом attention mask!), max pooling. Mean обычно лучше CLS: CLS без дообучения на целевую задачу плохо отражает смысл.
- С дообучением под похожесть: **Sentence-BERT** — сиамская схема, общий BERT, pooling → contrastive / triplet / MultipleNegativesRanking loss (in-batch negatives). Похожие тексты сближаются в косинусной метрике.
- Сырые эмбеддинги BERT **анизотропны** (лежат в узком конусе) → косинус между любыми текстами высокий, для поиска без дообучения работают плохо.

**⚠️ В сливе.** Не сказано про анизотропию и почему «просто CLS» для похожести — плохой выбор. Нет современных вариантов (SimCSE, E5 / bge-подобные модели, instruction-эмбеддеры).

**🎯 Follow-up.** Bi-encoder vs cross-encoder (скорость vs качество; retrieve → rerank). Hard negatives. Как искать по миллионам эмбеддингов (ANN: HNSW, IVF-PQ). **Кейс Авито: поиск дубликатов объявлений** — очень вероятен.

**📚** DL-151 (InfoNCE) · **дыра:** SBERT, bi/cross-encoder, ANN — нет нигде

---

### #22 Редкое слово в BERT · DS3 · `bert`
**Q.** В тексте редкое слово («подкрадули»). Даст ли BERT разумный эмбеддинг?

**Ядро ответа.** Да, по двум причинам: (1) **WordPiece** разобьёт его на частые подслова (`под`, `##крад`, `##ули`) — OOV в классическом смысле нет, в худшем случае посимвольно; (2) эмбеддинг **контекстный** — смысл частично восстанавливается из окружения. Вектор слова — pooling по его подсловам (mean, или первый подтокен).

**🎯 Follow-up.** WordPiece vs BPE vs Unigram (SentencePiece). Почему английский BERT плохо работает с русским (токенизатор режет русский почти посимвольно → длинные последовательности, плохие эмбеддинги) → нужны ruBERT / мультиязычные модели. Как размер словаря влияет на модель.

**📚** DL-108, DL-145 · **дыра:** алгоритмы BPE / WordPiece подробно

---

### #23 BERT для Question Answering · DS3 · `bert`
**Q.** Вопрос + текст, нужно найти фрагмент-ответ. Как дообучить BERT?

**Ядро ответа.**
- Вход: `[CLS] вопрос [SEP] контекст [SEP]`, segment-эмбеддинги различают части.
- На каждый токен контекста — два линейных слоя (вектора $S$ и $E$): $p_{\text{start}}(i) = \text{softmax}_i(S^\top h_i)$, $p_{\text{end}}(j)$ аналогично. Loss — сумма двух кросс-энтропий по позициям.
- Инференс: не два независимых argmax, а $\arg\max_{i\le j,\; j-i \le L_{\max}} (S^\top h_i + E^\top h_j)$.
- Нет ответа (SQuAD 2.0): ответ «указывает» на `[CLS]`, сравниваем скор с порогом.
- Длинный контекст: скользящее окно со страйдом.

**⚠️ В сливе.** «Берём argmax начала и argmax конца» — независимые argmax могут дать конец раньше начала. Правильно — максимум по допустимым парам.

**🎯 Follow-up.** Как бы решал это сейчас (LLM + RAG, extractive vs generative). Метрики QA: EM и F1 по токенам.

**📚** **дыра** — нигде нет

---

### #24 batch_size = 2048 при памяти на 32 · DS3 · `NLP`
**Q.** Хотим батч 2048, влезает 32. Что делать?

**Ядро ответа.** **Gradient accumulation:** на каждом микро-батче `loss / 64` → `backward()` (градиенты копятся), `optimizer.step()` + `zero_grad()` раз в $2048/32 = 64$ шагов.

**⚠️ В сливе.** Не сказано: (1) loss надо **делить на число шагов накопления**, иначе эффективный lr в 64 раза больше; (2) это **не** эквивалентно настоящему батчу 2048 при **BatchNorm** — статистики считаются по 32 (для трансформеров с LayerNorm неважно — стоит это сказать!); (3) для contrastive loss с in-batch negatives накопление **не даёт** 2047 негативов — нужны другие трюки (GradCache, memory bank).

**🎯 Follow-up.** Другие способы экономить память: mixed precision (bf16), gradient checkpointing, 8-bit оптимизаторы, ZeRO / FSDP, LoRA. Как менять lr при увеличении батча (linear scaling, warmup).

**📚** DL-24, DL-37, DL-38, DL-119

---

## 5. Продвинутые (DL) (2)

### #25 Регуляризация нейросетей, dropout на инференсе · DS2 · `DL`
**Q.** Какие способы регуляризации нейросетей знаешь. Как работает dropout на инференсе.

**Ядро ответа.**
- Weight decay (L2 / AdamW), dropout, early stopping, аугментации, label smoothing, stochastic depth, mixup; нормализации (BatchNorm / LayerNorm / GroupNorm) — это прежде всего про стабильность обучения, регуляризующий эффект у BatchNorm побочный (шум от статистик батча).
- Dropout: на обучении обнуляем нейрон с вероятностью $p$. Чтобы матожидание активации совпадало: либо на инференсе умножаем на $(1-p)$, либо (**inverted dropout, как в PyTorch**) на обучении делим на $(1-p)$, а на инференсе dropout выключен (`model.eval()`).
- Batch / Layer / Instance / Group Norm: по каким осям считается статистика. Для NLP — **LayerNorm**, потому что длины разные и батчи маленькие.

**⚠️ В сливе.** BatchNorm подан как основной метод регуляризации — это спорно, лучше явно разделить «нормализация» и «регуляризация».

**🎯 Follow-up.** Почему в трансформерах LayerNorm, а не BatchNorm. Что будет, если забыть `model.eval()` (dropout работает, BatchNorm использует статистики батча). MC-dropout для оценки неопределённости.

**📚** DL-17, DL-18, DL-19, DL-44, DL-46, DL-208

---

### #26 Классификация аккаунта по неупорядоченному окружению · DS3 · `DL` ⭐
**Q.** Недобросовестные продавцы плодят аккаунты с похожими объявлениями. Для аккаунта есть окружение — похожие аккаунты по техническим признакам (тот же IP и т.п.), их число произвольно. Какую архитектуру взять для классификации аккаунта по окружению?

**Ядро ответа.**
- Вход — **множество** переменного размера, порядок не важен → нужна **инвариантность к перестановкам**.
- **Deep Sets:** $f(X) = \rho\!\left(\sum_{x\in X}\phi(x)\right)$ — сумма (mean / max) симметрична, поэтому порядок не важен, а размер произвольный.
- **Attention без позиционных кодировок** (Set Transformer): self-attention эквивариантен к перестановкам, затем пулинг → инвариантность. Плюс — учитывает взаимодействие соседей между собой.
- **GNN (GAT / GraphSAGE):** аккаунты — узлы, общие IP / устройства / контакты — рёбра; attention по соседям взвешивает, какие связи важнее. Естественно масштабируется на многошаговое окружение.
- Альтернатива: найти самый похожий аккаунт в окружении и учить модель на паре (сиамская сеть). Но тогда нужно поймать случай «много умеренно похожих соседей» — это тоже подозрительно, отсюда агрегаты по всему окружению (число соседей, распределение похожестей).

**🎯 Follow-up.** Как получить разметку (баны модераторов, слабая разметка). Как не утечь по графу (соседи из будущего, забаненные соседи в признаках). Как масштабировать на миллионы аккаунтов (neighbor sampling). Как совместить с текстовыми эмбеддингами объявлений — **это мост к NLP**.

**📚** DL-111, DL-112, DL-113 · Deep Sets / Set Transformer — **дыра**

---

## 6. Карта покрытия: что брать из других подготовок

| Блок слива | Что уже готово | Состояние |
|---|---|---|
| #1–#8 основы ML | `ml_sobes` блоки 1–4 (Б-1…Б-39) | ✅ полностью, идёт подготовка к Яндексу |
| #9–#13 ансамбли | `ml_sobes` блок 5 (Б-40…Б-53) | ✅ полностью, кроме байесовской оптимизации гиперпараметров |
| #14–#15 CV | `deeeeeeep` DL-63…DL-72 | ✅ теория есть, достаточно повторить |
| #16 статья | — | ❌ подготовить 2 статьи |
| #17 эмбеддинги | `deeeeeeep` DL-132…DL-146 | 🟡 word2vec / GloVe / FastText есть, ELMo — проверить |
| #18–#20 трансформер | `deeeeeeep` DL-99, DL-102…DL-109 | ✅ есть, нужен дрил |
| #21–#23 практика BERT | DL-108, DL-151 частично | ❌ SBERT, bi/cross-encoder, QA-голова, WordPiece — писать с нуля |
| #24 память / батч | DL-24, DL-37, DL-38 | 🟡 есть, дописать нюансы |
| #25 регуляризация DL | DL-17…DL-19, DL-44, DL-46 | ✅ |
| #26 множества / графы | DL-111…DL-113 | 🟡 GNN есть, Deep Sets нет |
| Кейсы модерации (#4, #7, #26) | `ml_sobes` блоки 7–8 (Б-64…Б-78) | 🟡 каркас MLSD есть, NLP-кейсов нет |

## 7. Чего нет в сливе, но для NLP DS в 2026 вероятно

Слив — вспоминания прошедших, и NLP в нём застрял на BERT. Под NLP-команду Авито (модерация текстов, поиск, категоризация, дубли, чат-боты / LLM-фичи) стоит закрыть:

1. **Токенизация:** BPE / WordPiece / Unigram, как строится словарь, проблемы с русским языком. (DL-108 частично)
2. **LLM-стек:** decoder-only предобучение, instruction tuning, RLHF / DPO, **LoRA**, декодирование (greedy / beam / top-p / температура), KV-кэш. (DL-114…DL-121 — готово)
3. **RAG и retrieval:** bi-encoder + ANN + cross-encoder reranker, метрики ранжирования (Recall@k, MRR, nDCG). (DL-122 частично, остальное — дыра)
4. **Классификация текстов в проде:** TF-IDF + логрег как бейзлайн, fine-tuning BERT, дистилляция, мультиклассовая / мультилейбл, шумная разметка, дисбаланс, active learning. (DL-144, DL-217 частично)
5. **Оценка LLM и генерации:** BLEU / ROUGE и их проблемы, LLM-as-judge, галлюцинации. (DL-123, DL-131)
6. **NLP-кейсы в формате MLSD:** модерация объявлений, поиск дубликатов, автокатегоризация, извлечение атрибутов (NER), антиспам в мессенджере.
7. **Секции вне ML — уточнить у рекрутера:** алгоритмическая задача / Python-кодинг, SQL, статистика и A/B (для DS-позиции в Авито обычно встречаются; у тебя есть `99_ab_stats_cheatsheet.md` в `ml_sobes`).

## 8. Решения по плану (24.09)

- **Собес — неделя 28.09–02.10**, раньше Яндекса. Секции: ML (классика + DL CV / NLP) и Python (leetcode medium + язык).
- **Классический ML** — не дублируем: ежедневный ML-экспресс по карточкам `ml_sobes` ([`tickets/90_ml_express.md`](tickets/90_ml_express.md)).
- **DL / NLP** — 39 карточек А-1…А-39 в формате `ml_sobes`, от базы к сложному ([`tickets/INDEX.md`](tickets/INDEX.md)).
- **Код** — не в этом плане, алгоритмы готовятся отдельно.
- **Статья (#16)** — отдельно не готовим; рассказ про Sentence-BERT встроен в А-30.

Календарь — [`PLAN.md`](PLAN.md).

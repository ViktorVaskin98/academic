# Машинное обучение — Билетник для собеседований

> Интегрированный курс: С. Николенко «Машинное обучение. Основы» (вероятностный фундамент) + К.В. Воронцов «Курс лекций» (классические алгоритмы) + дополнения по темам, частым на собеседованиях. Порядок — логический учебный, не по одной книге. Формулы — Unicode. Пометки: `[доп.]` — продвинутый/факультативный материал; `[собес]` — добавлено сверх книг, часто спрашивают на интервью. Ссылка `(требует: Билет N)` — билет опирается на предыдущий.

---

## Раздел 1. Введение и методология

**Билет 1. Машинное обучение и искусственный интеллект: соотношение понятий.**
Определение ML как обучения на опыте (на данных), а не программирования решения вручную. ML как раздел AI, ставший в последние десятилетия доминирующим (другие разделы: экспертные системы, онтологии, логическое программирование). Идея коннекционизма: сложное поведение из композиции простых объектов (модель нейрона Маккаллоха–Питтса, 1943: линейная комбинация входов + нелинейность). Почему «настоящий» AI превратился в ML: параметры невозможно задать «из головы».

**Билет 2. Ранняя история идеи искусственного разума и механические автоматы.**
Обзорно: големы, говорящие головы, автоматы аль-Джазари, андроиды Жаке-Дро (первый программируемый «контроллер» — каллиграф Шарль), шахматный «Турок» как мошенничество. Смысл для истории AI: механика ≠ разум; какие задачи оказались лёгкими (бросать камни/катапульты), а какие сложными (компьютерное зрение — отличать корабли).

**Билет 3. Ранняя математическая логика и мысленные эксперименты.**
Логическая машина Раймунда Луллия (ars magna, комбинаторика понятий), «мельница Лейбница» и арифмометр, панлогизм. Китайская комната Джона Сёрла: человек механически перекладывает карточки по алгоритму — возникает ли понимание и «кто» понимает? Различие сильного и слабого AI. Зачем: постановка вопроса о природе «понимания» у машин.

**Билет 4. Тест Тьюринга.**
Формулировка (эссе Computing Machinery and Intelligence, 1950): судья в письменном диалоге должен отличить человека от машины; тест пройден, если вероятность верного угадывания неотличима от ½. Исходная «имитационная игра» (мужчина/женщина/судья) и почему деталь потерялась. Антагонистический (adversarial) тест — судья активно ищет подделку. Свойство «всё или ничего», сложность практического применения. Зачем: первое научное определение AI.

**Билет 5. Перцептрон Розенблатта и критика Минского–Пейперта.**
Один перцептрон реализует h(w₁x₁+…+w_dx_d): при h(x)=x — линейная регрессия, при h(x)=σ(x)=1/(1+e⁻ˣ) — логистическая регрессия. Разделяющая поверхность всегда линейна (уравнение сводится к w·x = c). Критика: перцептрон не реализует XOR (рис. с двумя классами, неразделимыми одной гиперплоскостью). Решение: сеть перцептронов (нейросеть). Историческое последствие: несправедливо «похоронило» нейросети до конца 1970-х.

**Билет 6. Волны хайпа, «зимы ИИ» и экспертные системы.**
Дартмутский семинар (1956) — рождение AI как науки. Первая волна и первая зима (машинный перевод, Джорджтаунский эксперимент, отчёт ALPAC 1966). Экспертные системы (MYCIN, ~600 правил медицинской диагностики, факторы уверенности, нечёткая логика). Вторая волна — обратное распространение ошибки (популяризация Румельхарта–Хинтона) и вторая зима. Третья волна (сер. 2000-х) — глубокое обучение + GPU + большие датасеты. Смена парадигмы 1990-х: единственный путь — обучение на данных.

**Билет 7. Классификация задач машинного обучения.**
Три типа постановок: обучение с учителем (supervised, D={(xₙ,yₙ)}, обучаем p(y|x)), обучение без учителя (unsupervised, D={(xₙ)}, обучаем p(x)), обучение с подкреплением (reinforcement, агент собирает D, обучаем стратегию π). Важно: это классификация постановок задач (что на входе/выходе), а не методов их решения. «Применить обучение с подкреплением» — некорректная фраза.

**Билет 8. Обучение с учителем: регрессия и классификация.**
Целевая переменная y: непрерывная (y∈ℝ) → регрессия, дискретная (класс) → классификация. В регрессии строим y≈f(x) и анализируем шум; в классификации — распределение вероятностей классов p(Cₖ|x). Функции ошибки различаются (происходят из максимизации правдоподобия/апостериорного). Промежуточные случаи: обработка последовательностей, обучение ранжированию (learning to rank — важен только порядок yₙ, не значения).

**Билет 9. Обучение без учителя.**
Датасет без меток D={(xₙ)}. Цель — обучить представление (representation) данных. Непрерывный случай: обучение представлений / понижение размерности (отображение в пространство меньшей размерности). Дискретный случай: кластеризация (разбиение на группы: внутри — похожи, между — различны). Semi-supervised learning (частичное привлечение учителя): малая размеченная + большая неразмеченная части.

**Билет 10. Обучение с подкреплением (обзорно).**
Агент действует в среде, накапливает опыт методом проб и ошибок и обучает стратегию π, максимизирующую выигрыш. Отличие от supervised/unsupervised: датасет не дан заранее, а собирается самим агентом. Прямое RL (обучаем π из опыта) и непрямое RL (обучаем модель мира). Проблема распределения вознаграждения (credit assignment), actor-critic (упоминаются в контексте Саттона).

**Билет 11. Извлечение признаков.**
Схема обработки сложных данных: вход x → извлечение признаков φ → простая модель на φ(x)∈ℝ^d → выход y. Простые линейные модели (объект книги) работают не на сырых данных, а на скрытых представлениях (latent representations, features). Революция глубокого обучения = автоматическое обучение φ вместо ручного подбора признаков. Пример: последний слой нейросети-классификатора — логистическая регрессия.

**Билет 12. Оверфиттинг и разбиение датасета.**
Оверфиттинг: модель «облизывает» обучающие данные вместо обобщения (идеальная «модель-БД» бесполезна). Нельзя оценивать модель на тех же данных, где обучались. Разбиение: обучающий набор (обучение), валидационный (подбор гиперпараметров, напр. силы регуляризации α), тестовый (сравнение моделей, смотреть нельзя). Типичная пропорция ~90:10:10, на Kaggle тест скрыт. `[собес]` утечка данных (data leakage), почему нельзя подбирать гиперпараметры на тесте.

**Билет 13. Кросс-валидация и утечки данных.** (требует: Билет 12)
`[собес]` Кросс-валидация (CV) — оценка обобщающей способности без отдельного теста. k-fold: разбить на k частей, k раз обучать на k−1 и валидировать на оставшейся, усреднить. Разновидности: stratified (сохранять доли классов), leave-one-out (k=ℓ, малосмещённая но дорогая и с большой дисперсией), time series split (только прошлое→будущее, без перемешивания). Nested CV: внешний цикл для оценки качества, внутренний для подбора гиперпараметров. Bootstrap .632. Утечки данных (data leakage): target leakage (признак содержит информацию о цели, недоступную в проде), утечка через препроцессинг (нормировку/импутацию/отбор признаков считать ТОЛЬКО на train внутри каждого fold), временная утечка, дубликаты между train/test. Симптом — подозрительно высокое качество на валидации.

**Билет 14. Типы входных данных.**
Табличные (x∈ℝ^d, малое d, слабозависимые признаки; предобработка, нормализация, дискретизация, one-hot encoding; деревья/бустинг/случайные леса хорошо жуют смешанные типы). Последовательности / временные ряды (x₁…x_T, зависимость по времени; тексты как последовательности токенов, эмбеддинги; звук, ASR/TTS, мультимодальность). Изображения (2D, computer vision, SLAM, сегментация, порождающие модели), 3D и видео.

**Билет 15. Кодирование категориальных признаков.**
`[собес]` One-hot (столбец на значение; взрыв размерности при высокой кардинальности). Ordinal/label (номер; вносит ложный порядок — опасно для линейных, ок для деревьев). Target/mean encoding (замена категории средним значением цели) — риск утечки, лечится out-of-fold расчётом и сглаживанием к глобальному среднему. Frequency/count encoding. Hashing trick (фиксированная размерность). Embeddings (обучаемые векторы, для нейросетей/высокой кардинальности). CatBoost — ordered target statistics (перестановка + префиксные статистики против утечки). Выбор: для деревьев часто ordinal/target, для линейных — one-hot/target.

**Билет 16. Применения машинного обучения в науках (обзорно).**
Физика/астрономия (обратные задачи = оценка параметров по данным; аппроксимация дорогих расчётов; поиск экзопланет по кривым блеска). Математика (AlphaTensor, AlphaDev, FunSearch; пока «умный перебор», полноценно теоремы не доказывает). Химия/биология (виртуальный скрининг молекул, drug discovery, «омики», медицинские изображения). Науки о Земле (кригинг = регрессия на гауссовских процессах, дистанционное зондирование). Соцнауки (осторожность: корреляция ≠ причинность, causal inference, Fragile Families Challenge).

## Раздел 2. Вероятностные основы

**Билет 17. Вероятностные пространства и распределения.**
Колмогоровская аксиоматика: пространство элементарных исходов Ω (S), события как подмножества. Дискретный случай: pmf p:S→[0,1], p(x)≥0, ∑p(x)=1. Непрерывный: алгебра и σ-алгебра (замкнутость относительно счётных объединений/пересечений/дополнений), борелевская σ-алгебра, вероятностное пространство ⟨S,𝒳,p⟩ как счётно-аддитивная мера с p(S)=1. Плотность (pdf) p(x): ∫_A p(x)dx = p(X∈A), ∫_S p(x)dx=1. Функция распределения (cdf) F(a)=∫_{−∞}^a p(x)dx, монотонна от 0 до 1. Дельта-функция Дирака δ(x); распределение данных p_data(x) = (1/N)∑ₙ δ(x−xₙ).

**Билет 18. Случайные величины и совместные распределения.**
Случайная величина X: измеримая функция S→ℝ (прообразы борелевских множеств измеримы). Позволяет присваивать числа исходам. Совместное распределение p(x,y) на декартовом произведении пространств; обобщение на n величин p(x₁,…,xₙ). Возможность соединять дискретные и непрерывные величины (смешанные векторы признаков). Образ меры под отображением (переход к стандартным пространствам).

**Билет 19. Условные вероятности и вывод теоремы Байеса.**
Определение p(X|Y) = p(X∩Y)/p(Y), корректно только при p(Y)>0. Парадокс Бореля–Колмогорова (условное распределение на большом круге сферы зависит от способа предельного перехода) — почему нельзя условиться при p(Y)=0. Условная плотность p(x|y)=p(x,y)/p(y). Формула полной вероятности p(X,Y)=p(X|Y)p(Y)=p(Y|X)p(X). Отсюда теорема Байеса: p(X|Y) = p(Y|X)p(X)/p(Y).

**Билет 20. Независимость и условная независимость.**
Независимость событий: p(X,Y)=p(X)p(Y). Независимость с.в.: F_{ξ,φ}(a,b)=F_ξ(a)F_φ(b), p(x,y)=p(x)p(y). Независимость в совокупности vs попарная (тетраэдр Бернштейна: попарно независимы, но не в совокупности). Зависимость ≠ причинность (пример: XOR-лампочка с проходными выключателями — B и S₁ независимы при детерминированной связи). Условная независимость p(X,Y|Z)=p(X|Z)p(Y|Z) — центральна для ML (данные условно независимы при условии параметров модели).

**Билет 21. Моменты случайной величины: ожидание и дисперсия.**
Ожидание E[ξ]=∑p(xᵢ)ξ(xᵢ) (дискр.) / ∫p(x)f(x)dx (непр.). Линейность E[αξ+βφ]=αE[ξ]+βE[φ]; не мультипликативно, но E[ξφ]=E[ξ]E[φ] при независимости. Санкт-петербургский парадокс (бесконечное ожидание, логарифмическая полезность). k-й момент μₖ=E[ξᵏ]. Дисперсия Var[ξ]=E[(ξ−E[ξ])²]=E[ξ²]−E[ξ]². Пример: Var равномерного = (b−a)²/12. Многомерно: вектор среднего μ=E[X], матрица ковариаций Cov(X)=E[XXᵀ]−E[X]E[X]ᵀ.

**Билет 22. [доп.] Альтернативные способы ввести понятие вероятности.**
Подход Эдвина Джейнса: начать с интуитивных требований к численной формализации достоверности (plausibility) логических высказываний. Ассоциативность F(x,F(y,z))=F(F(x,y),z) уже сильно ограничивает вид функции; после вывода получается w(F(x,y))=w(x)w(y), а из аксиом для достоверных/невозможных высказываний — обычная теория вероятностей (или эквивалентная репараметризация с w↦1/w). Смысл: другие базовые аксиомы, но та же формальная система.

**Билет 23. Теорема Байеса в машинном обучении.**
Форма p(θ|D) = p(θ)p(D|θ)/p(D) ∝ p(θ)p(D|θ). Термины: правдоподобие p(D|θ) (likelihood, модель = её функция правдоподобия), апостериорное p(θ|D) (posterior, «после опыта»), априорное p(θ) (prior, «до опыта»), полная вероятность / evidence p(D)=∫p(θ)p(D|θ)dθ (нормировочная константа). Смысл: как обновить представления после данных. Правило Кромвеля (Cromwell's rule): не присваивать нулевую априорную вероятность неисключённым событиям (иначе апостериорная навсегда 0).

**Билет 24. Медицинский тест и ошибка базовой доли.**
Нельзя «обратить» условную вероятность: p(t|d)≠p(d|t). По теореме Байеса p(d|t)=p(d)p(t|d)/[p(d)p(t|d)+p(d̄)p(t|d̄)]. Пример: тест с 95% точностью и превалентностью 1% → p(болен|+)≈16%. Ошибка базовой доли (base rate fallacy): игнорирование априорной вероятности p(d) (превалентности, base rate). Интуиция «на целых людях».

**Билет 25. Виды ошибок и метрики качества классификации.** (требует: Билет 24)
FP (ложноположительные, ошибка I рода), FN (ложноотрицательные, ошибка II рода), TP, TN. Метрики: чувствительность/полнота Sensitivity=Recall=TP/(TP+FN), специфичность Specificity=TN/(TN+FP), точность Precision=PPV=TP/(TP+FP), NPV=TN/(TN+FN), Accuracy=(TP+TN)/всего, FDR=FP/(TP+FP)=1−Precision, FOR=FN/(TN+FN), Prevalence=(TP+FN)/всего. На что влиять зависит от превалентности (при редкой болезни важнее снижать FP). `[собес]` F1 = 2·Precision·Recall/(Precision+Recall), ROC-кривая и AUC, precision-recall кривая, когда какая метрика уместна (несбалансированные классы).

**Билет 26. ROC-кривая, AUC, PR-кривая, F1.** (требует: Билет 25)
`[собес]` Классификатор выдаёт скор; порог задаёт баланс ошибок. ROC-кривая: TPR(=Recall) как функция FPR(=FP/N) при варьировании порога. ROC-AUC = площадь под ROC = вероятность, что случайный положительный получит больший скор, чем случайный отрицательный; 0.5 = случайно, 1 = идеально; инвариантна к базовым долям классов. PR-кривая (Precision-Recall) — предпочтительна при сильном дисбалансе (ROC-AUC может обманывать оптимистично). F1 = 2·P·R/(P+R) — гармоническое среднее; F_β взвешивает Recall в β² раз. Мультикласс: macro/micro/weighted усреднение. Log-loss (кросс-энтропия) штрафует уверенные ошибки. Порог выбирают под бизнес-стоимость ошибок.

**Билет 27. Три задачи байесовского вывода и смысл вероятностного подхода.**
(1) θ_ML = argmax_θ p(D|θ) — гипотеза максимального правдоподобия; (2) θ_MAP = argmax_θ p(θ|D) — максимальная апостериорная гипотеза; (3) предсказательное распределение p(x|D)=∫p(x|θ)p(θ|D)dθ — главная цель. Фриквентистский взгляд (вероятность как предел частоты) vs байесовский (степени доверия, degrees of belief) — нужен для неповторяемых событий. Условная независимость данных: p(D|θ)=∏ₙp(xₙ|θ). Зачем вероятностные модели: откуда берутся функции ошибки, границы применимости, интерпретация.

**Билет 28. Ошибка прокурора.**
Prosecutor's fallacy: приравнивание p(улика|невиновен) к p(невиновен|улика). Разница велика при высокой априорной невиновности и малой вероятности улики. Дело Салли Кларк (двойное перемножение вероятностей СВДС как независимых + ошибка прокурора, 1 к 73 млн). Детекторы лжи (полиграф) в суде бесполезны из-за высокой доли ложных срабатываний.

**Билет 29. Ошибка адвоката.**
Defense attorney's fallacy: корректный расчёт по Байесу, но с неверным (равномерным по всему городу) априорным распределением вины. Дело О. Джей Симпсона: (1) «стадион людей с той же кровью»; (2) статистика семейного насилия — оценили p(муж убил | бил в прошлом)≈1/2500 вместо p(муж убил | бил в прошлом И жена убита)≈90%.

**Билет 30. Парадокс Монти Холла и когнитивные искажения.**
Монти Холл: после открытия ведущим пустой двери выгодно менять выбор (выигрыш растёт с 1/3 до 2/3); интуиция — обмен одной двери на две. Ошибка игрока (gambler's fallacy). Эксперименты Канемана–Тверски: теория перспектив и неприятие риска (risk aversion), нарушение аксиом полезности; ошибка конъюнкции (conjunction fallacy, «Линда»: H⊂F, но H оценивают выше); ошибка планирования (planning fallacy).

**Билет 31. Байесовский анализ монетки: MLE и MAP.**
Испытания Бернулли, параметр θ=p(орёл). Правдоподобие p(D|θ)=θⁿ(1−θ)ᵐ. θ_ML=n/(n+m) (взятие производной). Проблема при n орлов и 0 решек (максимум уходит за [0,1]) — нужно ограничение области θ∈[0,1] через априорное распределение. Равномерное априорное p(θ)=1 на [0,1] даёт тот же θ_MAP=n/(n+m).

**Билет 32. Бета-распределение и гамма-функция.**
Бета-функция B(α,β)=∫₀¹ x^{α−1}(1−x)^{β−1}dx (интеграл Эйлера I рода). Гамма-функция Γ(α)=∫₀^∞ x^{α−1}e^{−x}dx, Γ(n+1)=n!, Γ(x+1)=xΓ(x). Связь B(α,β)=Γ(α)Γ(β)/Γ(α+β). Бета-распределение Beta(θ|α,β)=θ^{α−1}(1−θ)^{β−1}/B(α,β) на [0,1]. Апостериорное для монетки при равномерном априорном: p(θ|D)=Beta(θ|n+1,m+1).

**Билет 33. Предсказательное распределение и правило Лапласа.** (требует: Билет 32)
p(орёл|D)=∫θ·p(θ|D)dθ = E_{p(θ|D)}[θ]. Правило Лапласа (rule of succession): p(орёл|D)=(n+1)/(n+m+2). Дисперсия предсказания Var=(n+1)(m+1)/[(n+m+2)²(n+m+3)], убывает как ~1/(n+m); формула p ± √(p(1−p)/(n+m+3)). Применение в социологии: размер выборки для точности ±1% ≈ 2500.

**Билет 34. Сопряжённые априорные распределения.**
Семейство P сопряжённо к семейству правдоподобий L, если ∀p(θ)∈P, p(D|θ)∈L: p(θ)p(D|θ)∝p(θ|D)∈P. Бета сопряжено к Бернулли: Beta(θ|α,β)·θⁿ(1−θ)ᵐ ∝ Beta(θ|α+n,β+m) (вывод = сложение параметров). Позволяет последовательное обновление (хранить только параметры, не данные). Эквивалентный размер выборки (equivalent sample size): интерпретация α,β как виртуальных экспериментов.

**Билет 35. Игральные кости: мультиномиальное распределение и распределение Дирихле.**
Мультиномиальное: вектор θ=(θ₁…θ_K), θₖ≥0, ∑θₖ=1. One-hot кодирование категорий. Правдоподобие p(D|θ)=θ₁^{n₁}…θ_K^{n_K}. Распределение Дирихле Dir(θ|α) ∝ ∏θₖ^{αₖ−1} на симплексе, нормировка через многомерную бета-функцию B(α)=∏Γ(αₖ)/Γ(∑αₖ). Сопряжено к мультиномиальному: апостериорное Dir(θ|α+n). Дробные параметры α<1 добавляют разреженность (sparsity) — распределение перевзвешено к углам симплекса (одна вероятность близка к 1).

**Билет 36. [доп.] Case study: эффект «горячей руки».**
Hot hand в баскетболе. Работа Гиловича–Валлоне–Тверски: статистически «горячей руки» нет. Но эффект Миллера–Санхурхо (2015–2018): в конечной фиксированной последовательности бросков честной монетки доля орлов сразу после серии орлов систематически < 50% (селективное смещение отбора позиций). Разбор случая N=3. Важность: тонкая ошибка отбора, не противоречит независимости бросков; на рулетке так не выиграть.

**Билет 37. Кризис воспроизводимости в науке.**
Replication crisis (нач. 2010-х). Примеры: прайминг Барга (не воспроизвёлся), зефирный тест (эффект исчезает при учёте соц.-эконом. статуса), Стэнфордский тюремный эксперимент (инструктаж «охранников», фейковый нервный срыв), эксперименты Аша/Милгрэма (воспроизводятся, но часто искажённо цитируются). Мета-исследование 2015 (Science): значимость воспроизвелась в 36% из 100 работ vs 97% в оригиналах.

**Билет 38. p-значения и p-хакинг.**
p-значение = p(T≥t|H₀) (правосторонняя) / p(T≤t|H₀) / двусторонняя 2·min(...). H₀ — нулевая гипотеза (нет эффекта). Порог p<0,05. p-хакинг: подгонка данных/анализа до нужного p. Проблемы p-значений: это правдоподобие, а не апостериорная вероятность (ошибка прокурора); зависит от модели порождения; ничего не говорит о размере эффекта (effect size); множественные сравнения (multiple comparisons). `[собес]` доверительные интервалы, статистическая мощность, поправка Бонферрони.

**Билет 39. Сад расходящихся тропок и researcher degrees of freedom.**
Garden of forking paths (Гельман–Локен): даже один тест «нечестен», если он выбран после получения данных (метод зависит от данных → эффект множественных сравнений). Степени свободы исследователя: удаление выбросов, добавление переменных, выбор теста. Пререгистрация (preregistration) как средство. Параллель с ML: не использовать train как validation (оверфиттинг). Пример: парапсихология (Дэрил Бем, экстрасенсорное предвидение с p<0,05) как «контрольная группа науки».

**Билет 40. [доп.] Модель Иоаннидиса.**
«Why Most Published Research Findings Are False» (Ioannidis, 2005). Модель: априорные шансы существования зависимости R/(R+1), мощность 1−β, уровень ложных срабатываний α. Апостериорная вероятность истинности положительного результата (PPV) = R(1−β)/(R+α−βR). Учёт систематической ошибки (bias) и n независимых команд ухудшает PPV — при малой R большинство «открытий» ложны.

**Билет 41. Воспроизводимость в машинном обучении.**
Уроки кризиса для ML: пререгистрация экспериментов, честное разделение train/validation/test, отчёт о размере эффекта и дисперсии, публикация отрицательных результатов, опасность подбора гиперпараметров/архитектур под тестовый набор (аналог p-хакинга и оверфиттинга на бенчмарк).

## Раздел 3. Распределения и оптимизация

**Билет 42. Равномерное, биномиальное и геометрическое распределения.**
Дискретное равномерное (честная кость). Биномиальное Binom(k|n,p)=C(n,k)pᵏ(1−p)^{n−k} — число орлов из n бросков; E=np, Var=np(1−p) (сумма n Бернулли). Геометрическое Geom(k|p)=(1−p)ᵏp — число решок до первого орла. Для каждого — интуиция происхождения из испытаний Бернулли.

**Билет 43. Биномиальное распределение Пуассона.**
Poisson binomial — распределение числа успехов в n независимых, но неодинаковых испытаниях Бернулли (у каждого своя вероятность pᵢ). Обобщение биномиального (все pᵢ равны). E=∑pᵢ, Var=∑pᵢ(1−pᵢ). Когда возникает: агрегирование разнородных бинарных событий.

**Билет 44. Распределение Пуассона и закон редких событий.**
Пуассоновский процесс (независимость событий, постоянная интенсивность). Pois(λ): p(k|λ)=λᵏe^{−λ}/k!. Важное свойство: E=Var=λ. Закон редких событий: Binom(n,p) при n→∞, np→λ сходится к Pois(λ). Применение: моделирование счётных данных (число событий за интервал).

**Билет 45. Отрицательное биномиальное и гипергеометрическое распределения.**
Отрицательное биномиальное NegBinom(k|r,p)=C(k+r−1,r−1)(1−p)ᵏpʳ — число неудач до r-го успеха; в отличие от Пуассона разделяет среднее и дисперсию (лишний параметр, моделирует overdispersion). Гипергеометрическое HyperG: p(k|N,n,K)=C(K,k)C(N−K,n−k)/C(N,n) — выборка без замещения (k особых объектов из K в выборке n из N); пример с картами/покером.

**Билет 46. [доп.] Закон Ципфа.**
Zipf's law: p(k|s,N)=(1/H_{N,s})·(1/kˢ), H_{N,s} — обобщённое гармоническое число. Частота слова обратно пропорциональна его рангу. Выполняется для языков, размеров городов, доходов. Важное развенчание: закон Ципфа возникает даже в случайно порождённых текстах (не отражает глубокой закономерности). Обобщение Мандельброта.

**Билет 47. Непрерывное равномерное распределение.**
U[a,b]: плотность 1/(b−a) на [a,b]. E=(a+b)/2, Var=(b−a)²/12. Базовый непрерывный случай.

**Билет 48. Нормальное распределение и центральная предельная теорема.**
Гауссиан N(x|μ,σ²)=(1/√(2πσ²))·exp(−(x−μ)²/(2σ²)). E=μ, Var=σ². Стандартизация X=σZ+μ, Z∼N(0,1); функция распределения Φ (неэлементарная). Центральная предельная теорема: сумма/среднее большого числа независимых с.в. распределены нормально. Почему нормальное так важно (ЦПТ, максимум энтропии при фиксированных μ,σ², удобство).

**Билет 49. Логнормальное распределение и распределение Стьюдента.**
Логнормальное: X=e^{μ+σZ}, логарифм распределён нормально; возникает из мультипликативной ЦПТ (геометрическое среднее, закон Гибрата, распределение Гальтона). Распределение Стьюдента (t-distribution): параметр ν (число степеней свободы), тяжёлые хвосты (плотность ∝ полином степени −(ν+1)/2), при ν→∞ → гауссиан. Применения: доверительные интервалы для среднего малой выборки, робастная регрессия (устойчивость к выбросам).

**Билет 50. Гамма-распределение и экспоненциальное распределение.**
Gamma(x|α,β)=(βᵅ/Γ(α))·x^{α−1}e^{−βx} на (0,∞). Экспоненциальное = гамма с α=1 (максимум в x=0, экспоненциальный спад; время между событиями пуассоновского процесса). Гамма сопряжена к правдоподобию Пуассона (для интенсивности λ) и к точности (precision) гауссиана. Связь с отрицательным биномиальным (Пуассон со случайной гамма-интенсивностью).

**Билет 51. Немецкие танки: оценка параметра равномерного распределения.**
Задача о немецких танках (German tank problem): оценить N по серийным номерам (выборка без замещения из 1..N). Правдоподобие p(m|N)=1/N при N≥m. MLE=max наблюдённых номеров (смещённая, занижает). Частотная несмещённая оценка N̂ = m_max·(k+1)/k − 1. Метод «отметить и снова поймать» (mark-recapture, оценка Линкольна–Петерсена) для оценки популяции.

**Билет 52. Некорректные априорные распределения.**
Improper priors: распределение, которое нельзя нормировать (интеграл/сумма бесконечны), напр. равномерное на ℝ или на {k,…,∞}, или Beta(0,0)∝1/(θ(1−θ)). Формально не распределение, но использовать для вывода можно: апостериорное ∝ правдоподобию (с точностью до нормировки), если оно интегрируемо. Смысл: «не добавлять информацию сверх данных». Связь с эквивалентным размером выборки.

**Билет 53. [доп.] Пример Джейнса: урна с шарами.**
Джейнс критикует наивное отождествление «равномерное априорное = полное незнание». Разбор задачи с урной (N шаров, R красных, вынимаем n): как фоновое знание (background information) меняет вывод, и что именно означает «незнание». Подводит к принципу максимума энтропии и вопросу о правильных неинформативных априорных распределениях.

**Билет 54. Энтропия как мера неопределённости.**
Аксиоматика Шеннона (непрерывность, монотонность, свойство группировки) однозначно (с точностью до константы) задаёт функционал неопределённости. Энтропия H(p)=−∑pᵢ log pᵢ (дискр.) / −∫p log p (непр.). Максимальна для равномерного распределения (полное незнание). Почему не дисперсия (максимизация дисперсии даёт вырожденное решение).

**Билет 55. Перекрёстная энтропия и KL-дивергенция.**
Кросс-энтропия H(p,q)=−E_p[log q]. Теоретико-информационный смысл (биты для кодирования p кодом, оптимальным для q). Функция ошибки классификации L_CE=−∑yᵢ log ŷᵢ = −log ŷ_k (one-hot), сглаживание меток (label smoothing). KL-дивергенция KL(p‖q)=∫p log(p/q) = H(p,q)−H(p) ≥ 0 (неравенство Йенсена), =0 ⟺ p=q; несимметрична (не метрика). Дивергенция Джеффриса J=KL(p‖q)+KL(q‖p).

**Билет 56. KL-дивергенция в машинном обучении.** (требует: Билет 55)
Максимизация правдоподобия ⟺ минимизация KL(p_data‖p_model) (−E_{p_data}[log p_model] + const). Асимметрия KL важна: прямая KL(p‖q) — «mean-seeking»/mode-covering (накрывает все моды, пологая аппроксимация); обратная KL(q‖p) — «mode-seeking» (выделяет одну моду, острая). Пример аппроксимации бимодального распределения одним гауссианом. Значение для вариационного вывода.

**Билет 57. Информация Фишера и принцип максимума энтропии.**
Информация Фишера I(θ)=E_{x∼p(x|θ)}[(∂/∂θ log p(x|θ))²] = −E[∂²/∂θ² log p] — мера «остроты» правдоподобия вокруг максимума (сколько информации о θ несёт наблюдение). Принцип максимума энтропии (Джейнс): при заданной априорной информации I выбирать распределение с максимальной энтропией среди удовлетворяющих I. Решение через множители Лагранжа: pᵢ∝e^{−λi−μ} (пример с кубиком со средним 4,5). Максэнтропийные распределения = экспоненциальное семейство.

**Билет 58. [доп.] Априорные распределения Джеффриса.**
Prior Джеффриса p_J(θ)∝√(I(θ)) (в многомерном случае ∝√(det I(θ))). Свойство: инвариантность относительно репараметризации. Примеры: для монетки Beta(½,½); для среднего гауссиана — константа (плоское); для σ² гауссиана. Неинформативное априорное распределение, полученное из информации Фишера.

**Билет 59. Оптимизация в ML: градиентный спуск и невыпуклость.**
Обучение = оптимизация: θ_ML/θ_MAP = argmax функции (обычно −log правдоподобия/апостериорного). Целевая функция F(θ)=∑_{d} f(θ,d) (сумма из-за независимости данных). Невыпуклость (экспоненциально много локальных минимумов) — классическая теория оптимизации почти не применима, цель = локальный минимум. Методы: градиентный спуск (1-й порядок), метод Ньютона (2-й порядок, аппроксимация Тейлора). Расписания скорости обучения (линейное, экспоненциальное; температура T). Условия Вульфа, правило Армихо (line search). `[собес]` momentum, Adam, RMSProp.

**Билет 60. Стохастический градиентный спуск и его свойства.**
SGD: градиент по мини-батчу D_k вместо всего датасета D. Формализация как стохастическая оптимизация F(θ)=E_{q(y)}[f(θ,y)]; минимизация эмпирического риска. Оценка градиента по мини-батчу несмещённая, но с большой дисперсией (поэтому квазиньютоновские методы не переносятся). Проблемы: не движется точно по градиенту, не сходится без убывания скорости обучения (градиент не зануляется в минимуме). Условия сходимости (Роббинса–Монро): ∑αₖ=∞, ∑αₖ²<∞. `[собес]` batch size и его влияние, trade-off шум/скорость.

**Билет 61. Оптимизаторы: momentum, Adam, RMSProp.** (требует: Билет 60)
`[собес]` Улучшения SGD. Momentum: v_t=γv_{t−1}+η∇, θ=θ−v_t — накапливает скорость, гасит осцилляции в оврагах, ускоряет по пологим направлениям (γ≈0.9). Nesterov (NAG) — градиент в упреждающей точке. Адаптивные шаги по координатам: AdaGrad — делит на √∑g² (шаг затухает, плохо для долгого обучения); RMSProp — EMA квадратов градиента G_t=βG_{t−1}+(1−β)g², θ=θ−ηg/√(G_t+ε). Adam — объединяет momentum и RMSProp: m_t=β₁m_{t−1}+(1−β₁)g, v_t=β₂v_{t−1}+(1−β₂)g², коррекция смещения m̂=m/(1−β₁ᵗ), v̂=v/(1−β₂ᵗ), θ=θ−η·m̂/(√v̂+ε). AdamW — корректный weight decay. Практика: Adam — дефолт/быстрый старт; SGD+momentum часто даёт лучшее финальное обобщение. Расписания LR, warmup.

## Раздел 4. Линейные модели: регрессия

**Билет 62. Метод наименьших квадратов.**
Постановка: y(x,w)=w₀+w₁x₁+…+w_dx_d = wᵀx (фиктивная единица для w₀). Функция ошибки RSS(w)=∑ₙ(yₙ−wᵀxₙ)² = (y−Xw)ᵀ(y−Xw). Аналитическое решение (градиент = 0): нормальное уравнение XᵀXw*=Xᵀy, w*=(XᵀX)⁻¹Xᵀy при линейно независимых столбцах. Матрица (XᵀX)⁻¹Xᵀ — псевдообратная Мура–Пенроуза. Векторные формулы производных (∇(aᵀx)=a, ∇(xᵀx)=2x, ∇(xᵀAx)=2Ax для симметричной A).

**Билет 63. Коэффициент детерминации R².** (требует: Билет 62)
R² (coefficient of determination) — доля дисперсии зависимой переменной, объяснённая регрессией: R² = ∑(ŷₙ−ȳ)²/∑(yₙ−ȳ)² = 1 − SS_res/SS_tot. Смысл, диапазон значений. `[собес]` adjusted R², почему R² растёт при добавлении признаков.

**Билет 64. Функции признаков в линейной регрессии.**
Линейность по весам, а не по x: заменяя x на φ(x) (базисные функции), можно моделировать нелинейные зависимости, оставаясь в рамках линейной регрессии. Полиномиальная регрессия (φ(x)=(1,x,x²,…)). Модель y=wᵀφ(x)+ε. Все формулы МНК сохраняются (заменяем X на матрицу признаков Φ).

**Билет 65. Локальные признаки.**
Локальные базисные функции: радиальные базисные функции (RBF, гауссовы «горбы»), сплайны, кусочно-линейные признаки. Каждый признак «отвечает» за свою область входного пространства. Позволяют гибко приближать сложные функции линейной моделью.

**Билет 66. Оверфиттинг в полиномиальной регрессии.**
При росте степени d многочлена модель начинает подстраиваться под шум; характерный симптом — рост коэффициентов по модулю (десятки/сотни для данных из [−2,2]). Иллюстрация мотивации регуляризации. Связь с bias-variance (Билет 106).

**Билет 67. Гребневая регрессия.** (требует: Билет 62, 66)
Ridge regression: L(w)=∑(yₙ−wᵀxₙ)²+α‖w‖² (L2-регуляризатор). Решение w*=(XᵀX+αI)⁻¹Xᵀy. α — коэффициент регуляризации (подбор на валидации; w₀ обычно не регуляризуют). Регуляризация по Тихонову (Tikhonov): стабилизирует плохо обусловленные (ill-conditioned) / недоопределённые задачи. Сжимает веса, но не обнуляет.

**Билет 68. Лассо-регрессия.** (требует: Билет 67)
Lasso (least absolute shrinkage and selection operator): регуляризатор α∑|wᵢ| (L1-норма). Эффект разреженности (sparsity): при больших α многие коэффициенты становятся строго нулевыми → автоматический отбор признаков (feature selection). Нет аналитического решения (недифференцируема в нуле).

**Билет 69. Геометрия регуляризации.** (требует: Билет 67, 68)
Эквивалентная задача условной оптимизации: min RSS при R(w)≤β. Геометрия: линии уровня RSS (эллипсы) касаются области ограничения. L1-шар (ромб) имеет углы на осях → решение попадает на ось (обнуление коэффициентов, разреженность); L2-шар (круг) гладкий → веса сжимаются, но не зануляются. `[собес]` Elastic Net (L1+L2), выбор между ridge и lasso.

**Билет 70. Вероятностная интерпретация: гауссовский шум и МНК.**
Линейная регрессия — дискриминирующая (discriminative) модель p(y|x). Модель: t=y(x,w)+ε, ε∼N(0,σ²), т.е. p(t|x,w,σ²)=N(t|wᵀx,σ²). Мотивация гауссовского шума — ЦПТ. Логарифм правдоподобия датасета: −(N/2)ln(2πσ²) − (1/2σ²)∑(tₙ−wᵀxₙ)². Вывод: максимизация правдоподобия по w ⟺ метод наименьших квадратов. Оценка шума: σ²_ML = (1/N)∑(tₙ−w_ML ᵀxₙ)² (выборочная дисперсия остатков).

**Билет 71. Все вероятностные предположения линейной регрессии.** (требует: Билет 70)
(1) Условная независимость точек данных при условии w; (2) гауссовский шум; (3) гомоскедастичность (постоянная дисперсия шума σ², не зависит от x); (неявно) линейность зависимости. Ограничения гауссовского шума: симметричность (плохо для несимметричных данных, напр. финансовых) и тонкие хвосты (чувствительность к выбросам). Зачем понимать предположения: замечать их нарушения и модифицировать модель.

**Билет 72. Интерпретация коэффициентов: корреляция и причинность.**
Веса регрессии показывают корреляцию, а не причинность. Для одномерного x коэффициент w₁ связан с коэффициентом корреляции Пирсона r = ∑(xₙ−x̄)(yₙ−ȳ)/√(∑(xₙ−x̄)²∑(yₙ−ȳ)²). Confounders (скрытые общие причины). Причинный вывод (causal inference) — отдельная область. `[собес]` мультиколлинеарность и её влияние на интерпретацию коэффициентов.

**Билет 73. [доп.] Робастная регрессия с распределением Стьюдента.** (требует: Билет 70)
Замена гауссовского шума на шум Стьюдента (тяжёлые хвосты) → функция ошибки −∑log(ν+(y−wᵀx)²)·(ν+1)/2 вместо квадратичной. Устойчивость к выбросам: большие остатки штрафуются логарифмически, а не квадратично. Нет аналитического решения (нужна оптимизация).

**Билет 74. [доп.] Робастная регрессия: RANSAC и оценка Тейла–Сена.**
RANSAC (Random Sample Consensus): итеративно строить модель по случайному минимальному подмножеству, считать «инлайеров» (в пределах порога), выбрать модель с наибольшим консенсусом. Оценка Тейла–Сена (Theil–Sen): медиана попарных наклонов — точка срыва (breakdown point) ≈ 1−1/√2 ≈ 0,293 (устойчива к ~30% выбросов). Повторяющаяся медианная оценка (Сигел).

**Билет 75. Байесовский вывод в линейной регрессии.** (требует: Билет 70)
p(w|t,X) ∝ p(w)p(t|X,w). Гауссовское априорное p(w)=N(w|0,σ₀²I) → MAP-оценка эквивалентна гребневой регрессии (α=σ²/σ₀²). Лапласово априорное → лассо. Гауссово априорное сопряжено гауссову правдоподобию: апостериорное распределение весов тоже гауссово (можно найти точно среднее и ковариацию). Регуляризация = априорное предположение о малости весов.

**Билет 76. Предсказательное распределение в линейной регрессии.** (требует: Билет 75)
p(y|x,D)=∫p(y|x,w)p(w|D)dw — интегрирование по апостериорному распределению весов. Даёт не точечное предсказание, а распределение с дисперсией, растущей вдали от данных (учёт неопределённости параметров + шума). Семплы из апостериорного = набор правдоподобных прямых.

**Билет 77. [доп.] Оценка Джеймса–Штейна.**
Понятия: риск оценки R(µ̂,µ)=E‖µ̂−µ‖², доминирование, допустимая (admissible) / недопустимая оценка. Парадокс Стейна: оценка µ̂=x (MLE) для n независимых гауссианов допустима при n≤2, но недопустима при n≥3. Оценка Джеймса–Штейна µ̂_JS=(1−(n−2)/‖x‖²)x — «сжимает» оценку к нулю (shrinkage), строго доминирует над MLE. Интерпретации: геометрия высоких размерностей, эмпирический Байес (гауссово априорное), сокращение дисперсии ценой смещения (bias-variance).

**Билет 78. Case study: линейная регрессия и коронавирус.**
Экспоненциальный рост эпидемии → линейная регрессия на логарифмической шкале (log числа случаев линеен по времени). Байесовский подход: предсказание с доверительными интервалами (семплы из апостериорного), обучение функции распределения (не только среднего). Квадратичные признаки на лог-шкале для учёта замедления. Иллюстрация практической ценности вероятностного подхода.

## Раздел 5. Линейные модели: классификация

**Билет 79. Геометрия классификации: разделяющие поверхности.**
Разделяющая поверхность (decision surface) — множество точек, где решение меняется. Линейная разделяющая поверхность — гиперплоскость wᵀx+w₀=0 (w — нормаль). Нелинейные поверхности. Геометрический смысл классификации: проекция на нормаль и разделение в 1D. Расстояние от точки до гиперплоскости.

**Билет 80. Классификация для нескольких классов.**
Проблемы построения многоклассовой границы линейными методами: «каждый против каждого» (one-vs-one), «каждый против остальных» (one-vs-rest) — оба дают неоднозначные области. Решение: линейные оценочные функции aₖ(x)=wₖᵀx и правило argmax — разделяющие поверхности сходятся, области выпуклы (максимум линейных функций).

**Билет 81. Линейный дискриминант Фишера.**
Идея: спроецировать на направление w так, чтобы классы разделились. Не серединный перпендикуляр (учитывает только центры), а учёт внутриклассового разброса. Критерий Фишера J(w)=wᵀS_B w/wᵀS_W w (S_B — межклассовая матрица рассеяния, S_W — внутриклассовая). Решение w ∝ S_W⁻¹(m₂−m₁). Максимизация межклассового разброса при минимизации внутриклассового.

**Билет 82. Порождающие модели для классификации: LDA и QDA.**
По теореме Байеса p(Cₖ|x)=p(x|Cₖ)p(Cₖ)/∑ⱼp(x|Cⱼ)p(Cⱼ). Априорные p(Cₖ) — веса классов (равные или по частотам). Порождающая модель p(x|Cₖ)=N(x|µₖ,Σₖ). QDA (quadratic discriminant analysis): разные Σₖ → квадратичная граница. LDA (linear discriminant analysis): общая Σ → линейная граница. Если предположения верны — оптимальный байесовский классификатор. Регуляризация ковариаций (приближение к единичной).

**Билет 83. Логистическая регрессия: как из линейной функции получить вероятности.**
Проблема: p(C1|x) должна быть в [0,1], линейная функция — нет. Решение: моделировать логарифм шансов (log-odds, logit) log[p(C2|x)/p(C1|x)] линейно. Шансы (odds) = отношение вероятностей классов. Получаем p(C1|x)=σ(wᵀx), где σ(a)=1/(1+e⁻ᵃ) — логистический сигмоид (следует из теоремы Байеса).

**Билет 84. Максимизация правдоподобия в логистической регрессии.** (требует: Билет 83)
Правдоподобие p(D|w)=∏σ(wᵀxₙ)^{tₙ}(1−σ(wᵀxₙ))^{1−tₙ}. Логарифм = кросс-энтропия ∑[tₙ ln σ(wᵀxₙ)+(1−tₙ)ln(1−σ(wᵀxₙ))]. В отличие от линейной регрессии, нет аналитического решения (сигмоиды). Градиент прост: ∇=∑(σ(wᵀxₙ)−tₙ)xₙ. Оптимизация: градиентный спуск / метод итеративных перевзвешенных наименьших квадратов (IRLS = метод Ньютона).

**Билет 85. Другие сигмоиды и пробит-регрессия.**
Любая функция распределения (монотонная от 0 до 1) — валидный сигмоид с вероятностной интерпретацией. Пробит-регрессия: p(C1|x)=Φ(wᵀx), где Φ — функция распределения стандартного гауссиана. Интерпретация через латентную переменную (порог). Логистическая vs пробит: похожие результаты, разные хвосты.

**Билет 86. Лапласовская аппроксимация.**
Приближение сложного (унимодального) распределения p(z)=f(z)/Z гауссианом в окрестности моды. Метод: разложение log-плотности по Тейлору до 2-го порядка вокруг максимума → гауссиан со средним в моде и ковариацией = обратный гессиан (−H⁻¹) логарифма плотности. Применение: байесовский вывод в логистической регрессии (интегралы не берутся аналитически). Даёт и оценку нормировочной константы Z.

**Билет 87. [доп.] Предсказательное распределение в логистической регрессии.** (требует: Билет 86)
Интегрирование предсказания по апостериорному распределению весов (аппроксимированному лапласовски). Свёртка сигмоида с гауссианом (пробит-аппроксимация). Учёт неопределённости весов делает предсказания менее уверенными вдали от данных.

**Билет 88. SVM: принцип оптимальной разделяющей гиперплоскости.** (требует: Билет 83)
Линейный классификатор a(x)=sign(⟨w,x⟩−w₀). Отступ (margin) объекта M_i=(⟨w,x_i⟩−w₀)y_i (>0 — верно, <0 — ошибка). Для линейно разделимой выборки нормируем так, что min_i M_i=1. Тогда ширина разделяющей полосы = 2/‖w‖. Принцип максимального зазора (max margin): среди всех разделяющих гиперплоскостей выбрать с максимальной полосой → ½‖w‖²→min при условии M_i≥1 ∀i. Опорные векторы — объекты на границе полосы (M_i=1). Интуиция: широкий зазор = устойчивость и хорошее обобщение.

**Билет 89. SVM: мягкий зазор и hinge loss.** (требует: Билет 88)
Для линейно неразделимой выборки вводим переменные ξ_i≥0: M_i≥1−ξ_i. Задача: ½‖w‖²+C∑ξ_i → min. Эквивалентная безусловная форма: ∑(1−M_i)_+ + (1/2C)‖w‖² → min, где (1−M)_+ — hinge loss (кусочно-линейная верхняя оценка пороговой ошибки [M<0]), а ‖w‖² — L2-регуляризатор. Параметр C — компромисс между шириной зазора и числом нарушений (большой C → мало ошибок, узкий зазор, риск переобучения; малый C → широкий зазор). C обратно пропорционален силе регуляризации. Другие аппроксимации порога дают другие модели (логарифмическая → логистическая регрессия).

**Билет 90. SVM: двойственная задача и опорные векторы.** (требует: Билет 89)
Строим функцию Лагранжа для задачи мягкого зазора (множители α_i к M_i≥1−ξ_i, η_i к ξ_i≥0) и применяем условия ККТ. Условия седловой точки: w=∑α_i y_i x_i, ∑α_i y_i=0, α_i+η_i=C. Подстановка даёт двойственную задачу: ∑α_i − ½∑_i∑_j α_iα_j y_iy_j⟨x_i,x_j⟩ → max при 0≤α_i≤C, ∑α_i y_i=0. Опорный вектор — объект с α_i≠0. Типизация по ККТ: периферийные (α_i=0, M_i>1 — не влияют), опорные-граничные (0<α_i<C, M_i=1), опорные-нарушители (α_i=C, M_i<1). w₀ — из любого граничного. Обучение: SMO (Platt). Итоговый классификатор a(x)=sign(∑α_i y_i⟨x,x_i⟩−w₀) зависит только от опорных векторов.

**Билет 91. Ядра в SVM и спрямляющие пространства.** (требует: Билет 90)
В двойственной задаче и в предсказании объекты входят ТОЛЬКО через скалярное произведение ⟨x_i,x_j⟩. Заменяем его на ядро K(x_i,x_j)=⟨φ(x_i),φ(x_j)⟩ — скалярное произведение в спрямляющем пространстве H (feature space), не вычисляя φ явно (kernel trick). Это даёт нелинейные разделяющие поверхности. Условие Мерсера: K симметрична и положительно полуопределена (матрица Грама неотрицательно определена). Примеры ядер: линейное ⟨x,x′⟩; полиномиальное (⟨x,x′⟩+1)^d; RBF (гауссово) exp(−‖x−x′‖²/2σ²) (бесконечномерное H); сигмоидное. SVM с RBF-ядром ≈ двухслойная RBF-сеть, где число нейронов = число опорных векторов. Параметры (σ, d, C) подбирают по CV.

**Билет 92. Эквивалентное ядро в линейной регрессии.**
Предсказание линейной регрессии в точке x можно записать как взвешенную сумму обучающих ответов ŷ(x)=∑ₙk(x,xₙ)yₙ, где k — эквивалентное ядро (equivalent kernel), зависящее от матрицы признаков. Другой взгляд: линейная регрессия локально усредняет данные с весами-ядрами (smoother matrix).

**Билет 93. Ядерные методы.** (требует: Билет 92)
Ядро (kernel) k(x,x')=Φ(x)ᵀΦ(x') — функция похожести = скалярное произведение в пространстве признаков H. Ядерный трюк (kernel trick): строить алгоритмы через k(x,x'), не переходя явно в (возможно бесконечномерное) H. Требование: k задаёт скалярное произведение (положительная определённость, теорема Мерсера). Пример: классификатор по центроидам в H. `[собес]` RBF-ядро, SVM, RKHS.

**Билет 94. [доп.] SVM-регрессия и метод релевантных векторов (RVM).** (требует: Билет 90)
SVM-регрессия (ε-SVR): ε-нечувствительная функция потерь (|y−a(x)|−ε)_+ — ошибки внутри ε-трубки не штрафуются, что даёт разреженность (опорные векторы = точки вне трубки). Регуляризаторы для отбора признаков: L1 в SVM зануляет неинформативные признаки (методы SFM/RFM). RVM (relevance vector machine) — байесовский аналог SVM с ARD-приорами (automatic relevance determination), автоматически зануляющими большинство весов. Даёт вероятностные предсказания и обычно ещё разреженнее SVM, но обучение дороже и невыпукло.

**Билет 95. Порождающие и дискриминирующие модели.**
Дискриминирующие (discriminative): обучают p(y|x) напрямую. Порождающие (generative): обучают p(x,y)=p(y|x)p(x), могут порождать новые объекты. Обучить p(x) обычно сложнее, чем p(y|x). Когда достаточно дискриминативной модели, когда нужна порождающая (генерация, обучение представлений, модели мира). Максимизация правдоподобия p_model ⟺ минимизация KL(p_data‖p_model).

**Билет 96. Наивный байесовский классификатор.** (требует: Билет 95)
Простейшая порождающая модель для классификации. «Наивное» предположение: признаки условно независимы при условии класса p(y,x)=p(y)∏ᵢp(xᵢ|y). Обучение параметров как вероятностей костей (по частотам). Сглаживание Лапласа (θ̂=(#+1)/(#+M)) — правило Кромвеля против нулевых вероятностей. Классификация текстов (мешок слов, bag of words). Порождающе-дискриминирующая пара с логистической регрессией (то же семейство p(y|x), но разный способ обучения).

**Билет 97. Несбалансированные классы и перевзвешенная ошибка.**
Проблема: редкий класс (C_min) обычно и есть цель (аномалии, болезни, мошенничество). Порождающие модели (LDA/QDA) не страдают (баланс в p(Cₖ)). Геометрические методы страдают. Решения: undersampling (недосемплирование C_maj), oversampling (пересемплирование C_min, риск оверфиттинга), SMOTE (синтетические точки = выпуклые комбинации с соседями), перевзвешивание ошибки, изменение порога. `[собес]` метрики для несбалансированных данных (PR-AUC вместо ROC-AUC).

**Билет 98. Калибровка классификаторов.**
Хорошо откалиброванный (well-calibrated) классификатор: p(x∈Cₖ|h(x)=q)=q (среди предсказаний с вероятностью 20% событие происходит в 20% случаев). Недо-/переуверенные классификаторы. Оценка: диаграмма надёжности (reliability diagram), показатель Брайера (Brier score), ожидаемая ошибка калибровки (ECE), надёжность (REL). `[собес]` Platt scaling, temperature scaling, изотоническая регрессия.

**Билет 99. Как изменить классификатор в новых условиях.**
Адаптация при изменении априорного распределения / превалентности (prior shift): пересчёт порога π'. Формула коррекции при изменении соотношения классов (напр. добавление отрицательных примеров). Связь с калибровкой и несбалансированными классами.

**Билет 100. [доп.] Проспективные и ретроспективные исследования.**
Различие способов сбора данных: проспективные (выборка из популяции, естественная превалентность) vs ретроспективные (case-control, фиксированное число случаев). Как схема сбора влияет на оцениваемые вероятности и априорные распределения классов; какие метрики инвариантны к схеме.

**Билет 101. Ирисы Фишера.**
Классический датасет (3 вида ирисов, 4 признака). Разведочный анализ данных (EDA), парные диаграммы. Сравнение классификаторов (LDA, QDA, логистическая регрессия, kNN) на реальных данных. Иллюстрация практического применения методов главы.

## Раздел 6. Непараметрика, деревья, композиции

**Билет 102. Метод ближайших соседей.**
kNN — непараметрический метод. Гиперпараметр k, метрика d(x,xₙ). Классификация: голосование среди k соседей (нечётное k; веса ∝ 1/d). Регрессия: усреднение (выпуклая комбинация). Метрики: евклидова, Хэмминга, косинусная. Ускорение поиска: k-d деревья. Частный случай общего метода взвешенной суммы значений. `[собес]` диаграммы Вороного, ленивое обучение (lazy learning).

**Билет 103. Проклятие размерности.** (требует: Билет 102)
Curse of dimensionality: с ростом размерности d число ячеек сетки шага ε растёт экспоненциально ((l/ε)ᵈ); ожидаемое расстояние между случайными точками растёт (∝√d), «ближайшие» соседи становятся далёкими. Данных для покрытия пространства нужно экспоненциально много. Локальные методы (kNN) хороши в интерполяции, но не в высоких размерностях.

**Билет 104. Эффект кожуры апельсина.** (требует: Билет 103)
В высоких размерностях почти весь объём шара сосредоточен в тонком слое у поверхности (доля объёма во внутреннем шаре радиуса (1−ε) равна (1−ε)ᵈ → 0). Следствие: типичная точка выборки лежит дальше от центра, чем истинное среднее (связь с оценкой Джеймса–Штейна). Гауссова масса «убегает» от центра в высоких размерностях.

**Билет 105. Функция регрессии и оптимальные предсказания.**
При квадратичной функции потерь L=(y−f(x))² оптимальная функция предсказания — функция регрессии f̂(x)=E_{p(y|x)}[y] (условное ожидание). При 0-1 потере в классификации оптимален байесовский классификатор (самый вероятный класс). Ожидаемая ошибка предсказания EPE(f)=E_{p(x,y)}[(y−f(x))²]. kNN приближает функцию регрессии непараметрически (двойное приближение: E→среднее по соседям, точка→окрестность).

**Билет 106. Разложение на смещение, дисперсию и шум.** (требует: Билет 105)
EPE(f) = Bias² + Variance + Noise. Bias² = E[(f̂(x)−E_D f(x))²] — систематическое отклонение усреднённой по датасетам модели от истины. Variance = E[(E_D f(x)−f(x;D))²] — разброс модели от датасета к датасету. Noise = E[(f̂(x)−y)²] — неустранимый шум данных. Компромисс (trade-off): рост выразительности модели ↓Bias, ↑Variance; регуляризация ↑Bias, ↓Variance. `[собес]` связь с оверфиттингом/андерфиттингом, double descent.

**Билет 107. Логические закономерности и информативность.**
Предикат ϕ:X→{0,1} «покрывает» объект x при ϕ(x)=1. Закономерность — предикат, выделяющий много объектов одного класса c и мало «чужих». Обозначения: P,N — число своих/чужих в выборке; p(ϕ),n(ϕ) — сколько из них выделено. Критерии информативности: (1) эвристический — через E_c=n/(p+n) (доля негативных) и D_c=p/ℓ (охват); (2) статистический I_c (гипергеометрический — вероятность случайно получить такое разделение); (3) энтропийный — информационный выигрыш IGain=Ĥ(P,N)−Ĥ_ϕ(P,N,p,n), где Ĥ — энтропия деления на классы. Критерии асимптотически эквивалентны. Ценность правил — интерпретируемость.

**Билет 108. Решающие деревья: алгоритм ID3.** (требует: Билет 107)
Синтез дерева минимальной сложности — NP-полная задача; на практике жадный алгоритм «разделяй и властвуй» ID3. Рекурсия LearnID3(U): если U чистая — лист; иначе выбрать предикат β с максимальной информативностью, разбить U=U₀∪U₁ по β, рекурсивно строить поддеревья. Предикаты обычно пороговые [f_j≷d_j]. Критерий ветвления = прирост информативности (information gain через энтропию, или индекс Джини для CART). В листе — апостериорные вероятности классов по долям объектов. Трудоёмкость O(B·h·ℓ) (линейна по ℓ). Достоинства: интерпретируемость (путь=правило-конъюнкция), скорость, безошибочность на обучении. Недостатки: жадность (локально≠глобально оптимально), переусложнение и переобучение, ненадёжность в глубоких вершинах, высокая чувствительность к выборке. `[собес]` CART, Джини vs энтропия, деревья для регрессии (дисперсионный критерий).

**Билет 109. Редукция деревьев и обработка пропусков.** (требует: Билет 108)
Борьба с переобучением дерева. Предредукция (pre-pruning) — ранний останов ветвления, если информативность лучшего предиката ниже порога I₀; проще, но жадно-неоптимальна. Постредукция (post-pruning) — построить полное дерево, затем по независимой контрольной выборке (~30%) заменять внутренние вершины листом или одной из дочерних, если это сокращает число ошибок на контроле; обычно эффективнее. Пропуски: на обучении объект с неопределённым β не учитывается в информативности (критерий нормируют на длину подвыборки); на классификации — пропорциональное распределение (объект пропускается по обеим ветвям с весами p̂_L=|U₀|/|U|, p̂_R, апостериорные вероятности классов усредняются рекурсивно).

**Билет 110. [доп.] Решающие списки и голосование правил.** (требует: Билет 107)
Решающий список (decision list) — упорядоченная последовательность правил «если ϕ_k(x) то класс c_k», применяется первое сработавшее; строится жадно (на каждом шаге ищется самая чистая закономерность, покрытые объекты удаляются). Взвешенное голосование правил — набор закономерностей голосует за классы с весами (алгоритмы КОРА, ТЭМП, бустинг над правилами). Дерево тривиально преобразуется в список (путь до листа = правило). Отличие деревьев от списков: деревья не дают отказов от классификации.

**Билет 111. Композиции алгоритмов.**
При недостаточном качестве одного алгоритма строят композицию, где ошибки базовых взаимно компенсируются. Общий вид (алгебраический подход Журавлёва): a(x)=C(F(b₁(x),…,b_T(x))), где b_t — базовые алгоритмические операторы X→R (пространство оценок), F — корректирующая операция, C — решающее правило. Простое голосование; взвешенное голосование ∑α_t b_t (выпуклая комбинация при α_t≥0, ∑α_t=1). Условие полезности: базовые алгоритмы должны быть (а) достаточно точными (лучше случайного) и (б) достаточно РАЗНЫМИ. Способы обеспечить различность: перевзвешивание объектов (бустинг), случайные подвыборки/подпространства (бэггинг/RSM), разные инициализации.

**Билет 112. Бэггинг и случайный лес.** (требует: Билет 106)
Бэггинг (bagging = bootstrap aggregating, Брейман): из выборки длины ℓ бутстрепом (выбор с возвращением) формируют T подвыборок той же длины (в каждой ~1−1/e≈63.2% уникальных объектов), базовые алгоритмы обучаются НЕЗАВИСИМО, объединяются простым голосованием/усреднением. Эффект — снижение ДИСПЕРСИИ (в терминах bias-variance) за счёт усреднения разных моделей; смещение почти не меняется. Поэтому бэггинг полезен для неустойчивых моделей с низким смещением и высокой дисперсией (глубокие деревья). Out-of-bag оценка качества. RSM (random subspace method) — случайные подмножества признаков. Случайный лес (Random Forest) = бэггинг глубоких деревьев + случайный выбор √n признаков в каждой вершине (декорреляция деревьев). Легко распараллеливается.

**Билет 113. Бустинг: AdaBoost.** (требует: Билет 106, 112)
Бустинг строит взвешенное голосование a(x)=sign∑α_t b_t(x) ПОСЛЕДОВАТЕЛЬНО, концентрируясь на трудных объектах. Пороговую ошибку [M<0] аппроксимируют сверху экспонентой e^{−M}; минимизируется Q_T=∑_i exp(−y_i ∑_t α_t b_t(x_i)). Эвристики: (1) на шаге t настраивается только b_t,α_t; (2) веса объектов w_i=exp(−y_i·частичной суммы). Теорема (AdaBoost): при отсутствии отказов оптимум при b_T=argmin N(b;Wℓ) (взвешенная ошибка) и α_T=½ln((1−N)/N), где N — суммарный вес ошибок. Алгоритм: w_i:=1/ℓ; на каждом шаге обучить b_t по взвешенной ошибке, вычислить α_t, пересчитать w_i:=w_i·exp(−α_t y_i b_t(x_i)), нормировать. Трудные объекты накапливают вес. Сходимость за конечное число шагов при γ-преимуществе. Эффект — снижение СМЕЩЕНИЯ. Замечания: фильтрация выбросов (объекты с макс. весом), неожиданно высокая обобщающая способность (качество на тесте растёт даже после нулевой ошибки на обучении). Бустинг над деревьями — один из сильнейших методов.

**Билет 114. Градиентный бустинг.** (требует: Билет 113)
Обобщение бустинга на произвольную дифференцируемую функцию потерь L (Фридман). Ансамбль a_T(x)=∑_t γ_t b_t(x) строится как градиентный спуск в ПРОСТРАНСТВЕ ФУНКЦИЙ: для текущего приближения a_{T−1,i} вычисляем антиградиент (псевдоостатки) −g_i=−∂L(a_{T−1,i},y_i)/∂a. Новый базовый алгоритм обучается приближать антиградиент по МНК: b_T=argmin_b ∑_i(b(x_i)+g_i)²; затем одномерный поиск шага γ_T=argmin_γ ∑_i L(a_{T−1,i}+γb_T(x_i),y_i). Для квадратичной потери антиградиент = обычные остатки y_i−a_{T−1,i}. Стохастический градиентный бустинг (SGB): случайная подвыборка на каждом шаге (как бэггинг). Shrinkage (темп обучения ν<1): a_T=a_{T−1}+ν·γ_T b_T. GBDT = градиентный бустинг над решающими деревьями. Гиперпараметры: число деревьев, глубина, learning rate, subsample.

**Билет 115. XGBoost, LightGBM, CatBoost.** (требует: Билет 114)
`[собес]` Промышленные реализации GBDT. XGBoost: разложение потерь по Тейлору до 2-го порядка (градиент + гессиан), регуляризация сложности дерева (γ·#листьев + ½λ∑w²), аналитически оптимальные веса листьев и критерий сплита, гистограммное построение сплитов, встроенная обработка пропусков, shrinkage, subsample/colsample, ранняя остановка. LightGBM: leaf-wise рост дерева (не level-wise), GOSS (отбор объектов по градиенту), EFB (объединение разреженных признаков) — быстрее и экономнее на больших данных. CatBoost: упорядоченный бустинг (ordered boosting против target leakage), нативные категориальные признаки (ordered target statistics), небрежные (oblivious) симметричные деревья. Де-факто стандарт для табличных данных.

**Билет 116. Стэкинг и блендинг.**
Нелинейное ансамблирование: комбинировать базовые модели обучаемым мета-алгоритмом, а не фиксированным голосованием. Стэкинг (stacking): мета-модель обучается на предсказаниях базовых, полученных ПО OUT-OF-FOLD схеме (иначе мета-признаки переобучены — утечка). Блендинг — то же на отдельной отложенной выборке (проще, но теряет данные). Линейный стэкинг (взвешенный по признакам). Смеси экспертов (mixture of experts) с функциями компетентности/gating — вес базовой модели зависит от объекта. Отличие от голосования: мета-модель учится, КОГДА какой базовой доверять.

## Раздел 7. Байесовский вывод и обобщения

**Билет 117. Case study: байесовский вывод для гауссиана.**
Полный байесовский вывод для среднего µ и точности τ=1/σ² гауссиана. Сопряжённое априорное — нормально-гамма (Normal-Gamma). Выводы: для фиксированного τ (гауссово апостериорное для µ), для фиксированного µ (гамма для τ), совместно. Маргинализация апостериорного распределения. Предсказательное распределение получается распределением Стьюдента (t-distribution) — тяжёлые хвосты из-за неопределённости в τ.

**Билет 118. Маргинальное правдоподобие и эмпирический Байес.**
Маргинальное правдоподобие / функция свидетельства (evidence) p(D|α,β)=∫p(w|α)p(D|w,β)dw — знаменатель теоремы Байеса на уровне гиперпараметров. Эмпирический байес (empirical Bayes) = максимизация правдоподобия II типа (type II MLE, MLE-II): подбор гиперпараметров максимизацией маргинального правдоподобия. «Нечестность»: использует тот же датасет. Применение: подбор силы регуляризации α, β в линейной регрессии.

**Билет 119. Оценка p(D) как метод сравнения моделей.** (требует: Билет 118)
Маргинальное правдоподобие p(D|M) как критерий сравнения моделей: предпочесть модель с наибольшим свидетельством. Автоматическая бритва Оккама (Occam's razor): сложная модель «размазывает» вероятностную массу по большему пространству данных, поэтому проще модель, адекватно описывающая данные, выигрывает без явного штрафа за сложность.

**Билет 120. Байесовский информационный критерий.** (требует: Билет 119)
BIC(M) = −2 log p(D|θ_ML, M) + d·log N (d — число параметров, N — размер данных). Выводится лапласовской аппроксимацией маргинального правдоподобия. Штраф d·log N за сложность модели. Минимизируем BIC для выбора модели. `[собес]` AIC = −2 log p(D|θ_ML) + 2d (слабее штрафует сложность); AIC vs BIC (предсказание vs истинная модель).

**Билет 121. [доп.] Информационные критерии Такеучи и Акаике.**
AIC (Akaike information criterion) как оценка ожидаемой KL-дивергенции между истинным распределением и моделью; штраф 2d. TIC (Takeuchi information criterion) — обобщение AIC без предположения о правильной спецификации модели (использует матрицы Фишера и гессиан). Связь AIC/TIC/BIC.

**Билет 122. Экспоненциальное семейство.**
Определение: p(x|θ)=h(x)·exp(η(θ)ᵀt(x)−a(θ)). η(θ) — естественный параметр (при η(θ)=θ — естественная параметризация), t(x) — достаточные статистики, a(θ) — лог-нормировка (log-partition), h(x) — базовая мера. Ключевое свойство: θ и x связаны только через скалярное произведение в экспоненте. Примеры: Бернулли/биномиальное (естественный параметр = log-odds), мультиномиальное, гауссово, Пуассона, гамма, бета. Максэнтропийные распределения принадлежат экспоненциальному семейству.

**Билет 123. Свойства экспоненциального семейства.** (требует: Билет 122)
Моменты достаточных статистик через производные лог-нормировки: E[t(x)]=∇a(θ), Cov[t(x)]=∇²a(θ) (a(θ) — производящая функция кумулянтов). Максимизация правдоподобия: приравнять эмпирическое среднее достаточных статистик к теоретическому (1/N)∑t(xₙ)=E_θ[t(x)]. Универсальная конструкция сопряжённых априорных распределений и предсказательных распределений для всего семейства.

**Билет 124. Обобщённые линейные модели.** (требует: Билет 122)
GLM: ŷ=g⁻¹(wᵀx), линейный предиктор c=wᵀx, среднее µ=g⁻¹(c). g — функция связи (link function). Распределение отклика из (дисперсного) экспоненциального семейства со средним µ. Частные случаи: линейная регрессия (тождественная связь, гауссово), логистическая регрессия (logit-связь, Бернулли). Единый вид правдоподобия и оптимизации (IRLS).

**Билет 125. Пуассоновская регрессия.** (требует: Билет 124)
Poisson regression: моделирование счётных данных (число событий). Отклик y∼Pois(λ), логарифмическая связь λ=exp(wᵀx) (log link, гарантирует λ>0). Максимизация правдоподобия Пуассона. Применения: число покупок, посещений, событий.

**Билет 126. Отрицательная биномиальная регрессия.** (требует: Билет 125)
Negative binomial regression — обобщение пуассоновской для случая избыточной дисперсии (overdispersion, когда Var>E, что нарушает предположение Пуассона E=Var). Дополнительный параметр разделяет среднее и дисперсию. Интерпретация: Пуассон со случайной гамма-распределённой интенсивностью (гамма-пуассоновская смесь).

## Раздел 8. Обучение без учителя

**Билет 127. Непараметрическое восстановление плотности.**
Оценка p(x) без параметрической модели. Ядерная оценка Парзена–Розенблатта: p̂(x)=(1/ℓ)∑_i (1/V_h)·K((x−x_i)/h), где K — ядро (симметричная плотность), h — ширина окна, V_h — нормировка. Выбор ядра слабо влияет на результат; выбор ширины окна h КРИТИЧЕН: малое h → «иглы»/переобучение, большое h → пересглаживание. h подбирают по LOO-правдоподобию. Переменная ширина окна (адаптивная, k-NN плотность). Проклятие размерности бьёт по непараметрике сильно. В классификации — метод парзеновского окна (оценить p(x|C_k) ядрами и подставить в байесовское решающее правило).

**Билет 128. Разделение смеси распределений (GMM).**
Модель вероятностной смеси: p(x)=∑_{j=1}^k w_j·φ(x,θ_j), w_j≥0, ∑w_j=1. Двухуровневый порождающий процесс: выбрать компоненту j~w_j (дискретное априорное), затем породить x~φ(·,θ_j). Гауссова смесь (GMM): φ=N(μ_j,Σ_j). Скрытая переменная — номер компоненты, породившей объект. Критерий — максимум log-правдоподобия ∑_i ln∑_j w_j φ(x_i,θ_j). Из-за суммы под логарифмом нет замкнутого решения → EM-алгоритм. Применения: мягкая кластеризация (объект принадлежит компонентам с вероятностями), оценка плотности, генеративная модель, выделение подтипов.

**Билет 129. EM-алгоритм.** (требует: Билет 128)
Итеративный метод максимизации правдоподобия для моделей со скрытыми переменными. Для смеси: E-шаг (expectation) — оценить скрытые g_ij=P(компонента j | x_i)=w_j φ(x_i,θ_j)/∑_s w_s φ(x_i,θ_s) (формула Байеса, «мягкое» отнесение, ∑_j g_ij=1). M-шаг (maximization) — обновить параметры по взвешенному ММП: θ_j=argmax_θ ∑_i g_ij ln φ(x_i,θ) и w_j=(1/ℓ)∑_i g_ij (средний вес компоненты). Для GMM M-шаг даёт μ_j,Σ_j как g-взвешенные среднее и ковариацию. Вывод — через ККТ/лагранжиан с ограничением ∑w_j=1. Свойства: монотонный рост правдоподобия, сходимость к ЛОКАЛЬНОМУ максимуму (важна инициализация; несколько запусков). Общая трактовка: максимизация нижней оценки (ELBO). `[собес]` связь с вариационным выводом, вырождение компонент.

**Билет 130. Кластеризация: метод k-средних (k-means).** (требует: Билет 129)
Разбить X на K кластеров, минимизируя сумму внутрикластерных квадратов расстояний ∑_i ‖x_i−μ_{a_i}‖² → min по назначениям a_i и центрам μ_a. Алгоритм Ллойда: (1) отнести каждый x_i к ближайшему центру a_i=argmin_a‖x_i−μ_a‖; (2) пересчитать центры μ_a как средние объектов кластера; повторять до стабилизации. k-means = ЖЁСТКОЕ упрощение EM для GMM (сферические одинаковые Σ, жёсткое отнесение вместо мягких g_ij). Проблемы: выбор K (метод локтя по inertia, силуэт), чувствительность к инициализации (решение — k-means++), находит только выпуклые/сферические кластеры сопоставимого размера, сходимость лишь к локальному минимуму.

**Билет 131. Кластеризация DBSCAN.**
Плотностная кластеризация (Density-Based Spatial Clustering of Applications with Noise). Параметры: радиус ε и порог m. Типы точек по ε-окрестности U_ε(x): корневые (|U_ε(x)|≥m — плотная окрестность), граничные (не корневые, но в окрестности корневой), шумовые/выбросы (ни то, ни другое). Кластер = множество связанных по плотности корневых точек вместе с их граничными. Достоинства: кластеры произвольной формы (без центров!), число кластеров определяется автоматически, явное выделение выбросов, сложность O(ℓ log ℓ) при эффективном поиске соседей. Недостатки: чувствительность к ε,m; плохо при сильно разной плотности. `[собес]` HDBSCAN.

**Билет 132. Иерархическая кластеризация.**
Агломеративная (снизу вверх): начать с ℓ одноэлементных кластеров; итеративно сливать два ближайших; пересчитывать расстояния до нового кластера по формуле Ланса–Уильямса R_{W,S}=α_U R_{U,S}+α_V R_{V,S}+β R_{U,V}+γ|R_{U,S}−R_{V,S}|. Способы связи (linkage): ближнего соседа (single, «цепочки»), дальнего (complete, компактные), среднего (average), Уорда (Ward — минимум прироста внутрикластерной дисперсии). Результат — дендрограмма; число кластеров выбирается срезом на нужной высоте. Дивизимная — сверху вниз. Плюс: не требует задавать K заранее, даёт всю иерархию; минус: O(ℓ²–ℓ³), чувствительность к шуму.

**Билет 133. Оценка качества кластеризации.**
`[собес]` Внутренние метрики (без разметки): силуэт (silhouette — контраст внутри/между кластерами, ∈[−1,1]), индекс Дэвиса–Болдина (меньше лучше), Калински–Харабаса (больше лучше), inertia. Внешние (при известной разметке): ARI (adjusted Rand index, скорректирован на случайность), NMI/AMI (взаимная информация), homogeneity/completeness/V-measure. Выбор числа кластеров K: метод локтя (elbow по inertia), максимум силуэта, gap-статистика. Фундаментальная проблема: единственно «правильной» кластеризации не существует.

**Билет 134. Метод главных компонент (PCA).**
`[из головы]` Линейное снижение размерности без учителя: найти ортогональные направления максимальной дисперсии данных. Шаги: центрировать (и обычно стандартизировать) признаки; ковариационная матрица C=(1/ℓ)XᵀX; главные компоненты = собственные векторы C, упорядоченные по убыванию собственных значений λ_i; доля объяснённой дисперсии = λ_i/∑λ. Проекция z=Wᵀx, W — top-k собственных векторов. Эквивалентно минимизации ошибки реконструкции ‖x−WWᵀx‖². Важно стандартизировать признаки (иначе доминируют крупномасштабные). Применения: визуализация, декорреляция, шумоподавление, сжатие, предобработка. Kernel PCA — нелинейное обобщение. `[собес]` PCA (без учителя, максимум дисперсии) vs LDA (с учителем, максимум разделимости классов).

**Билет 135. Сингулярное разложение (SVD) и связь с PCA.** (требует: Билет 134)
`[из головы]` Любую матрицу X (ℓ×n) можно разложить X=UΣVᵀ, где U и V ортогональны, Σ — диагональ сингулярных чисел σ_i≥0. Связь с PCA: правые сингулярные векторы (столбцы V) = главные компоненты, а σ_i²/ℓ = собственные значения ковариации XᵀX/ℓ. Усечённое SVD (top-k σ) даёт наилучшее приближение ранга k (теорема Эккарта–Янга–Мирского по норме Фробениуса). Применения: латентно-семантический анализ (LSA/LSI), матричная факторизация в рекомендательных системах, сжатие, псевдообратная Мура–Пенроуза. Численно устойчивее, чем PCA через явную XᵀX.

**Билет 136. Нелинейное снижение размерности: t-SNE, UMAP.**
`[собес]` t-SNE: сохраняет ЛОКАЛЬНУЮ структуру — задаёт вероятности соседства в исходном пространстве (гауссовы) и в 2–3D (t-распределение с тяжёлыми хвостами против «скучивания», crowding problem), минимизирует KL между ними. Только для визуализации; не сохраняет глобальные расстояния; гиперпараметр perplexity; стохастичен. UMAP: на основе теории многообразий — быстрее t-SNE, лучше сохраняет глобальную структуру, годится как препроцессинг. Автокодировщики — обучаемое нелинейное обобщение PCA. Когда что: PCA — линейное/быстрое/интерпретируемое; t-SNE/UMAP — визуализация кластерной структуры.

**Билет 137. [доп.] Semi-supervised learning.**
Обучение с частичным привлечением учителя: малая размеченная + большая неразмеченная выборки. Предположения, делающие неразмеченные данные полезными: кластерное (объекты одного кластера — один класс), многообразия (данные на низкоразмерном многообразии), гладкости (близкие объекты → близкие ответы). Методы: модификации кластеризации (k-means, Ланс–Уильямс) с учётом меток; self-training (самообучение на псевдометках уверенных предсказаний); графовые методы (label propagation по графу похожести); трансдуктивный SVM. Различают трансдуктивное (предсказать только для данной неразмеченной выборки) и индуктивное (общий классификатор) обучение.

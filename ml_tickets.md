# Машинное обучение — билетник для собеса (v2, фокус на классический ML)

> Сфокусированный билетник под собесы/экзамен по классическому ML. Источник каждого билета помечен: `[Николенко]` — книга «Машинное обучение. Основы»; `[Воронцов]` — лекции (`data/books/voron/`); `[ИТМО]` — конспект курса (`data/Classic_ML.txt`, с номером лекции); `[собес]` — добавлено под интервью. Формулы — Unicode. `(требует: Билет N)` — опирается на предыдущий.

---

## Раздел 1. Линейная регрессия

**Билет 1. Формализация задачи машинного обучения.** _[ИТМО, лек.1]_
Три компоненты задачи: объекты x∈X, признаки (features) f(x)∈ℝ (числовое описание), целевая переменная y. Матрица «объект–признак» X размера [n×(m+1)]. Типы обучения: с учителем (supervised, y известны), без учителя (unsupervised, y нет — ищем структуру/кластеры), частичное (semi-supervised, y есть у части). Типы задач с учителем по виду y: регрессия (y∈ℝ), классификация (y — категория), ранжирование.

**Билет 2. Линейная регрессия: модель и метод наименьших квадратов.** _[ИТМО, лек.1 + Николенко]_
Модель: f(x,w)=w₀+w₁f₁(x)+…+w_m f_m(x)=wᵀx (фиктивная единица под w₀=bias). Матрично y=Xw. Критерий МНК: w=argmin‖y−Xw‖₂². Линейность по ВЕСАМ, а не по x: замена x на φ(x) (полиномиальные/RBF-признаки) моделирует нелинейности, оставаясь линейной моделью. Геометрия: проекция вектора ответов y на линейную оболочку столбцов X.

**Билет 3. Нормальное уравнение и матрица Грама.** _[ИТМО, лек.1]_
Аналитическое решение через ∇RSS=0: XᵀXw=Xᵀy → w=(XᵀX)⁻¹Xᵀy. XᵀX — матрица Грама (симметричная, положительно полуопределённая). Матрица (XᵀX)⁻¹Xᵀ — псевдообратная Мура–Пенроуза. Проблема: XᵀX обратима не всегда — при мультиколлинеарности или n<m определитель =0. Вычислительная сложность обращения O(m³) — дорого при большом числе признаков.

**Билет 4. Мультиколлинеарность и вырожденность матрицы Грама.** _[ИТМО, лек.1,2]_
Мультиколлинеарность — линейная зависимость (или почти) между признаками. Следствие: XᵀX вырождена/плохо обусловлена → веса «взрываются» (огромные по модулю, неустойчивые к шуму). При n<m (признаков больше объектов) решение не единственно. Способы борьбы: отбор признаков, регуляризация (Ridge добавляет τE — гарантирует обратимость), PCA (декорреляция), решение через псевдообратную (SVD, отбрасывание малых σ).

**Билет 5. Градиентный спуск для линейной регрессии.** _[ИТМО, лек.1]_
Когда данных много или XᵀX не обращается — итерационно: w_{k+1}=w_k−h∇L(w_k). Градиент МНК: ∇L=2Xᵀ(Xw−y). Learning rate h: большой → расходимость/перепрыгивание минимума, малый → медленно. Критерии останова: малый градиент/изменение w, лимит итераций. Аналитика vs GD: аналитика точна но O(m³) и требует обратимости; GD масштабируется на большие данные.

**Билет 6. Сингулярное разложение (SVD): определение и свойства.** _[ИТМО, лек.1,3]_
Любую X [n×m] можно разложить: X=USVᵀ, U,V — ортогональные, S=diag(σ₁≥…≥σ_r≥0) — сингулярные числа. Связь: σ_j²= собственные числа XᵀX; столбцы V — их собственные векторы. Геометрия: поворот (Vᵀ) → растяжение по осям (S) → поворот (U). Усечённое (truncated) SVD оставляет top-k σ. Число обусловленности cond=σ_max/σ_min.

**Билет 7. Решение МНК через SVD и псевдообратная матрица.** _[ИТМО, лек.1]_
Подстановка X=USVᵀ в нормальное уравнение: w=V(SᵀS)⁻¹SᵀUᵀy. Для вырожденного случая — сумма только по ненулевым σ: w=∑_{j=1}^r (1/σ_j)·V_j(U_jᵀy), r=rank(X). Это псевдообратная Мура–Пенроуза. Отбрасывание малых σ_j (усечение) повышает устойчивость: маленькие σ дают деление на почти-ноль → усиление шума. Численно надёжнее, чем явное обращение XᵀX.

**Билет 8. Метрики качества регрессии.** _[ИТМО, лек.1 + собес]_
MSE=(1/n)∑(yᵢ−ŷᵢ)² (штрафует выбросы квадратично). RMSE=√MSE (в единицах y). MAE=(1/n)∑|yᵢ−ŷᵢ| (устойчива к выбросам). MAPE=(1/n)∑|yᵢ−ŷᵢ|/|yᵢ| (в %, ломается при yᵢ≈0). R²=1−SS_res/SS_tot — доля объяснённой дисперсии (≤1, может быть <0). Метрика качества ≠ функция потерь при обучении. `[собес]` adjusted R², почему R² растёт при добавлении признаков.

**Билет 9. Предобработка признаков: масштабирование и кодирование.** _[ИТМО, лек.1 + собес]_
Масштабирование: стандартизация (z-score: (x−μ)/σ) и min-max нормализация — ускоряют сходимость GD (делают «чашу» круглее), нужны для методов на расстояниях (kNN, k-means, SVM) и сопоставимости весов. Кодирование категорий: one-hot (столбец на значение; dummy trap — убрать один столбец во избежание коллинеарности), ordinal (номер — ложный порядок), target/mean encoding (риск утечки — out-of-fold + сглаживание), hashing, embeddings. `[собес]` для деревьев масштабирование не нужно; ordinal/target ок.

**Билет 10. Вероятностный взгляд на линейную регрессию.** _[Николенко]_
МНК = метод максимального правдоподобия при гауссовском шуме: y=wᵀx+ε, ε∼N(0,σ²), т.е. p(y|x,w)=N(y|wᵀx,σ²). Логарифм правдоподобия ∝ −(1/2σ²)∑(yₙ−wᵀxₙ)² → максимизация по w эквивалентна МНК. L2-регуляризация (Ridge) = гауссово априорное p(w)=N(0,σ₀²I) (MAP-оценка); L1 (Lasso) = лапласово априорное; MAE = лапласовский шум. Предположения: независимость точек, гауссов шум, гомоскедастичность (постоянная σ²).

## Раздел 2. Регуляризация и теория оптимизации

**Билет 11. Число обусловленности и устойчивость решения.** _[ИТМО, лек.2]_
cond(A)=‖A‖·‖A⁻¹‖=σ_max/σ_min — во сколько раз относительная ошибка входа Δb усиливается в решении Ax=b. Большой cond → неустойчивость: мизерный шум в данных → огромные скачки весов. Геометрия: «почти параллельные прямые» — точка пересечения улетает от дрожания. Высокий cond матрицы Грама ⟺ мультиколлинеарность ⟺ взрыв весов. Лечится регуляризацией (τE снижает cond) и масштабированием.

**Билет 12. Переобучение и недообучение.** _[ИТМО, лек.2 + Николенко]_
Переобучение (overfitting): модель подстраивается под шум, идеально на train, плохо на test; симптом — огромные веса, плохая обусловленность. Недообучение (underfitting): модель слишком проста, высокая ошибка везде. Нельзя оценивать на данных обучения. Разбиение train/validation/test (валидация — подбор гиперпараметров, тест — только финальная оценка). Борьба с переобучением: регуляризация, больше данных, упрощение модели, отбор признаков, ранняя остановка.

**Билет 13. Ridge-регрессия (L2-регуляризация).** _[ИТМО, лек.2 + Николенко]_
L=(1/n)‖Xw−y‖²+τ‖w‖₂². Аналитическое решение: w=(XᵀX+τE)⁻¹Xᵀy. Добавка τE на диагональ гарантирует обратимость и улучшает обусловленность (регуляризация Тихонова). Сжимает веса к нулю, но НЕ обнуляет. Влияние τ: рост τ → ↑bias, ↓variance. w₀ (bias) обычно не штрафуют. Байесовски = гауссово априорное на веса.

**Билет 14. Lasso-регрессия (L1-регуляризация).** _[ИТМО, лек.2 + Николенко]_
L=(1/n)‖Xw−y‖²+λ‖w‖₁. Главное свойство: ОБНУЛЯЕТ веса неинформативных признаков → автоматический отбор признаков (sparsity), интерпретируемость. Нет аналитического решения (модуль недифференцируем в нуле) — координатный спуск/субградиент. Байесовски = лапласово априорное.

**Билет 15. Геометрия регуляризации: L1 vs L2.** _[ИТМО, лек.2]_
Эквивалент условной задачи: min RSS при R(w)≤β. Линии уровня RSS (эллипсы) касаются области ограничения. L1-шар (ромб) имеет углы НА ОСЯХ → решение попадает на ось (зануление, разреженность). L2-шар (круг) гладкий → веса сжимаются, но не зануляются. `[собес]` когда Ridge (все признаки полезны, коллинеарность), когда Lasso (нужен отбор, много мусорных признаков).

**Билет 16. Elastic Net и функция потерь Хубера.** _[ИТМО, лек.2]_
Elastic Net: комбинация L1+L2 (λ₁‖w‖₁+λ₂‖w‖₂²) — и отбор признаков, и устойчивость; хорош для групп коррелированных признаков (Lasso из группы берёт один случайный, Elastic Net — всю группу). Функция Хубера: квадратичная для малых остатков |r|≤δ, линейная для больших — устойчивость к выбросам (как MSE в центре, как MAE на хвостах). Сравнение по чувствительности к выбросам: MSE ≫ Huber > MAE.

**Билет 17. Выпуклость, L-гладкость и сильная выпуклость.** _[ИТМО, лек.2]_
Выпуклость: любой локальный минимум глобален (нет ловушек). L-гладкость (липшицев градиент, ‖∇f(x)−∇f(y)‖≤L‖x−y‖): градиент не меняется резко → безопасный шаг h≤1/L. μ-сильная выпуклость (f−μ/2‖x‖² выпукла): функция «крутая» везде, нет плато. Примеры: МНК выпукл и L-гладок (L=λ_max(XᵀX)); МНК+L2 μ-сильно выпукл (μ=τ). Эти свойства дают гарантии скорости.

**Билет 18. Скорость сходимости градиентного спуска.** _[ИТМО, лек.2]_
Для L-гладких μ-сильно выпуклых функций GD сходится ГЕОМЕТРИЧЕСКИ: ‖w^{k+1}−w*‖²≤qᵏ‖w⁰−w*‖², q<1. Число итераций до точности ε: K≈O((L/μ)·ln(1/ε)). Отношение L/μ = число обусловленности задачи = «вытянутость чаши»: чем вытянутее (хуже обусловленность), тем больше зигзагов и дольше сходимость; идеальная сфера (L=μ) — быстро. За один шаг нельзя (кроме Ньютона) — знаем лишь наклон под ногами. Практика: масштабирование делает чашу круглее, регуляризация ↑μ.

**Билет 19. Стохастический градиентный спуск (SGD) и mini-batch.** _[ИТМО, лек.2 + собес]_
SGD: градиент по одному объекту/мини-батчу вместо всей выборки. Дешёвая итерация, но шумная оценка градиента (несмещённая, но с большой дисперсией). Не сходится без убывания шага (градиент не зануляется в минимуме из-за шума). Условия Роббинса–Монро: ∑αₖ=∞, ∑αₖ²<∞. Расписание шага (learning rate decay). Незаменим на больших данных. `[собес]` влияние batch size: малый — шумно/регуляризует, большой — стабильно но дорого.

**Билет 20. Ускоренные и адаптивные методы: Momentum, Adam.** _[собес + ИТМО, лек.2]_
Momentum («тяжёлый шарик»): v_t=γv_{t−1}+η∇, θ=θ−v_t — накапливает скорость, гасит осцилляции в оврагах (γ≈0.9). Nesterov (NAG): градиент в упреждающей точке. Адаптивные (поэлементный шаг): AdaGrad (делит на √∑g², затухает); RMSProp (EMA квадратов g); Adam = momentum+RMSProp с коррекцией смещения: m̂=m/(1−β₁ᵗ), v̂=v/(1−β₂ᵗ), θ=θ−η·m̂/(√v̂+ε). AdamW — корректный weight decay. Практика: Adam — дефолт; SGD+momentum часто даёт лучшее финальное обобщение.

**Билет 21. Разложение на смещение, дисперсию и шум (bias-variance).** _[Николенко]_
EPE(f)=Bias²+Variance+Noise. Bias² — систематическое отклонение усреднённой по датасетам модели от истины (недообучение). Variance — разброс модели от датасета к датасету (переобучение). Noise — неустранимый шум данных. Trade-off: ↑сложность модели ⇒ ↓Bias, ↑Variance; регуляризация ⇒ ↑Bias, ↓Variance. Связь: бэггинг снижает Variance, бустинг — Bias. `[собес]` learning curves как диагностика, double descent.

**Билет 22. Энтропия, кросс-энтропия и KL-дивергенция.** _[Николенко]_
Энтропия H(p)=−∑p_i log p_i — мера неопределённости, максимум у равномерного распределения. Кросс-энтропия H(p,q)=−∑p_i log q_i. KL-дивергенция KL(p‖q)=∑p_i log(p_i/q_i)=H(p,q)−H(p)≥0 (неравенство Йенсена), =0 ⟺ p=q; НЕсимметрична (не метрика). Ключевые связи в ML: максимизация правдоподобия ⟺ минимизация KL(p_data‖p_model); кросс-энтропия = функция потерь логрег/softmax (log-loss); энтропия = критерий ветвления деревьев (information gain). `[собес]` асимметрия KL: прямая mean-seeking (накрывает моды), обратная mode-seeking (одна мода).

**Билет 23. Кросс-валидация и утечки данных.** _[собес]_
Оценка обобщения без отдельного теста. k-fold: k раз обучать на k−1 частях, валидировать на оставшейся, усреднить. Разновидности: stratified (доли классов), leave-one-out (k=n), time series split (только прошлое→будущее). Nested CV: внешний цикл — оценка, внутренний — подбор гиперпараметров. Утечки данных (data leakage): target leakage; препроцессинг (нормировку/импутацию) считать ТОЛЬКО на train внутри fold; временная утечка; дубликаты train/test. Симптом — подозрительно высокое качество.

## Раздел 3. Оптимизация второго порядка

**Билет 24. Метод Ньютона.** _[ИТМО, лек.4]_
Аппроксимация L(x) по Тейлору до 2-го порядка: L(x)≈L(xₖ)+⟨∇L,x−xₖ⟩+½⟨∇²L·(x−xₖ),x−xₖ⟩. Приравняв градиент аппроксимации к нулю: x_{k+1}=xₖ−[∇²L(xₖ)]⁻¹∇L(xₖ). ∇²L — гессиан. Плюс: квадратичная скорость сходимости вблизи оптимума (идёт «прямо в цель»). Для квадратичной функции — за один шаг.

**Билет 25. Проблемы метода Ньютона и Левенберг–Марквардт.** _[ИТМО, лек.4]_
Минусы Ньютона: вычисление гессиана дорого; обращение O(m³); гессиан может быть необратим/плохо обусловлен; вдали от минимума метод может расходиться. Левенберг–Марквардт (гибрид GD и Ньютона для нелинейного МНК): направление из (JᵀJ+λI)p=−Jᵀf, J — якобиан. λI (как в Ridge) гарантирует обратимость и регулирует переход: большой λ → градиентный спуск (надёжно, медленно), малый λ → Гаусс–Ньютон (быстро). λ адаптируют по успеху шага.

**Билет 26. Демпфированный метод Ньютона и линейный поиск.** _[ИТМО, лек.7]_
Демпфирование: x_{k+1}=xₖ+γₖ[H]⁻¹∇f — вводим размер шага γₖ и приближённый H (напр. гессиан, посчитанный один раз в x₀ — «замороженный»). Линейный поиск (line search): при известном направлении pₖ=−[H]⁻¹∇f задача сводится к одномерной γₖ*=argmin_γ f(xₖ+γpₖ). Точный поиск дорог → неточный (inexact): найти γ с достаточным убыванием (условия Армихо/Вульфа). Для унимодальной функции на луче — деление пополам / золотое сечение.

**Билет 27. Квазиньютоновские методы: секущее уравнение.** _[ИТМО, лек.7]_
Идея: не считать точный гессиан, а строить приближение обратного гессиана B_k≈[∇²f]⁻¹ по истории градиентов (только 1-е производные). Из разложения Тейлора для градиента: y_k≈∇²f·s_k, где s_k=x_{k+1}−xₖ, y_k=∇f(x_{k+1})−∇f(xₖ). Домножив на обратный гессиан — секущее уравнение: s_k=B_{k+1}y_k. Оно недоопределено (m условий на m² неизвестных) → доп. требования: близость B_{k+1} к B_k, симметричность, положительная определённость.

**Билет 28. Метод SR-1 (Symmetric Rank-1, Бройден).** _[ИТМО, лек.7]_
Добавка ранга 1: ΔB_k=μ_k q_k q_kᵀ. Подстановка в секущее уравнение даёт формулу: B_{k+1}=B_k+((s_k−B_k y_k)(s_k−B_k y_k)ᵀ)/((s_k−B_k y_k)ᵀy_k). Начало B₀=E (единичная). При k→∞ B_k сходится к обратному гессиану. Недостаток: может терять положительную определённость (знаменатель отрицателен) → направление не убывания.

**Билет 29. Алгоритм BFGS и L-BFGS.** _[ИТМО, лек.7]_
BFGS (Broyden–Fletcher–Goldfarb–Shanno) — самый мощный квазиньютоновский метод классического ML. Добавка ранга 2: B_{k+1}=(I−ρ_k s_k y_kᵀ)B_k(I−ρ_k y_k s_kᵀ)+ρ_k s_k s_kᵀ, ρ_k=1/(y_kᵀs_k). Гарантия: если B₀≻0 и y_kᵀs_k>0 (обеспечивает line search), то все B_k≻0 → направление всегда убывающее. L-BFGS (limited memory): хранит не всю матрицу, а последние m пар (s_k,y_k) — для больших размерностей. Сравнение: GD (1-й порядок, дёшево/долго) → Ньютон (2-й, дорого/быстро) → BFGS (золотая середина).

## Раздел 4. Снижение размерности

**Билет 30. Теорема о наилучшем низкоранговом приближении (Эккарта–Янга).** _[ИТМО, лек.3]_
Задача: min_{rank(B)=k} ‖X−B‖ (по норме Фробениуса/спектральной). Решение через SVD X=USVᵀ: B=∑_{j=1}^k u_jσ_j v_jᵀ (первые k компонент). Ошибка приближения = первое отброшенное σ_{k+1}. Зачем снижать размерность: борьба с шумом, визуализация, скорость, мультиколлинеарность, проклятие размерности.

**Билет 31. PCA через максимизацию дисперсии.** _[ИТМО, лек.3 + собес]_
PCA ищет ортогональные направления максимальной дисперсии проекций. Задача: max ‖X·Rot‖² при ‖Rot‖=1. Лагранжиан L=Rotᵀ(XᵀX)Rot−λ(RotᵀRot−1); ∇L=0 → (XᵀX)Rot=λRot. Вывод: главные компоненты = собственные векторы ковариационной матрицы XᵀX, λ=σ² — дисперсии проекций. Эквивалентность двух взглядов: «максимум дисперсии» = «минимум ошибки реконструкции ‖x−WWᵀx‖²». `[собес]` PCA (без учителя, дисперсия) vs LDA (с учителем, разделимость классов).

**Билет 32. PCA: геометрия, выбор числа компонент, центрирование.** _[ИТМО, лек.3]_
Переход X·Rot=K, где Rot=первые d правых сингулярных векторов V. Информативность: σ упорядочены σ₁≥…≥σ_r; сохранённая доля S_d/∑σ. Выбор d — scree plot (график σ_i): точка «локтя», где падение сменяется плато; хвост = шум. КРИТИЧНО центрировать данные (вычесть среднее) перед PCA, обычно и стандартизировать (иначе доминируют крупномасштабные признаки). Применения: визуализация, декорреляция, шумоподавление, предобработка.

**Билет 33. Степенной метод (Power Iteration) и deflation.** _[ИТМО, лек.3]_
Как найти главный собственный вектор без полного SVD: итерация x^{k+1}=Ax^k/‖Ax^k‖ (A=XᵀX) сходится к собственному вектору с максимальным собственным числом. Скорость зависит от отношения |λ₂/λ₁| (чем меньше, тем быстрее). Deflation (исчерпывание): для следующей компоненты вычесть вклад найденной A_new=A−σ₁v₁v₁ᵀ, повторить. Выгодно, когда нужно лишь несколько top-компонент из большой матрицы.

**Билет 34. Kernel PCA и ядра.** _[ИТМО, лек.3]_
Линейный PCA не берёт нелинейные структуры (кольца, спирали). Идея: отобразить x→φ(x) в пространство большей размерности, где структура линеаризуется, и применить PCA там. Ядра: полиномиальное (c+⟨x,y⟩)^d, RBF exp(−γ‖x−y‖²) (бесконечномерное пространство). Kernel trick: всё выражается через матрицу ядра K(xᵢ,xⱼ), явно φ(x) не вычисляем. Пример: «выдавить» центр листа в 3-е измерение z=x²+y² → кольца разделяются плоскостью.

**Билет 35. Нелинейное снижение размерности: t-SNE и UMAP.** _[собес + ИТМО, лек.3]_
t-SNE: сохраняет ЛОКАЛЬНУЮ структуру — вероятности соседства в исходном (гауссовы) и в 2–3D (t-распределение, тяжёлые хвосты против crowding), минимизирует KL. Только визуализация; не сохраняет глобальные расстояния; гиперпараметр perplexity; стохастичен; нет «из коробки» out-of-sample преобразования. UMAP: на основе теории многообразий — быстрее, лучше сохраняет глобальную структуру, годится как препроцессинг. Автокодировщики — обучаемое нелинейное обобщение PCA. PCA — когда нужно линейное/быстрое/интерпретируемое.

## Раздел 5. Классификация: линейные методы

**Билет 36. Задача классификации и отступ (margin).** _[ИТМО, лек.4]_
Бинарная (Y∈{−1,+1}) и многоклассовая (Y∈{1..K}). Дискриминантная функция c(x,w) — уверенность; ответ a(x)=sign(c(x,w)). Отступ (margin) M=c(x,w)·y: M>0 — верно, M<0 — ошибка, |M| — уверенность. Типизация объектов: M≫0 надёжный, M≈0 пограничный, M≪0 сильная ошибка (шум/выброс). Величина отступа лежит в основе функций потерь (SVM, бустинг).

**Билет 37. Пороговая функция потерь и её гладкие оценки.** _[ИТМО, лек.4]_
Идеальный функционал = доля ошибок Q₀=(1/n)∑[M<0]. Проблема: «ступенька» имеет нулевую производную везде → градиентные методы неприменимы. Решение: заменить гладкой верхней оценкой. Логистическая: log₂(1+e^{−M}) — логистическая регрессия, плавно штрафует даже уверенно-правильные. Hinge: max(0,1−M) — SVM, штраф =0 при M≥1. Экспоненциальная: e^{−M} — AdaBoost. Квадратичная: (1−M)². Разные оценки → разные методы.

**Билет 38. Логистическая регрессия.** _[Николенко + ИТМО, лек.4]_
Моделируем логарифм шансов (log-odds) линейно: log[p(C₁|x)/p(C₀|x)]=wᵀx → p(C₁|x)=σ(wᵀx), σ(a)=1/(1+e^{−a}) (следует из теоремы Байеса). Правдоподобие ∏σ(wᵀxₙ)^{tₙ}(1−σ)^{1−tₙ}; лог = кросс-энтропия (log-loss). Нет аналитики; градиент ∇=∑(σ(wᵀxₙ)−tₙ)xₙ; обучение GD или IRLS (=Ньютон). Интерпретация весов через odds ratio (e^{w_j}). `[собес]` калибрует вероятности «из коробки», L2/L1-регуляризация.

**Билет 39. LDA, QDA и линейный дискриминант Фишера.** _[Николенко]_
Порождающий подход: p(Cₖ|x)∝p(x|Cₖ)p(Cₖ), p(x|Cₖ)=N(μₖ,Σₖ). QDA: разные Σₖ → квадратичная граница. LDA: общая Σ → линейная граница. Дискриминант Фишера: спроецировать на w так, чтобы максимизировать межклассовый разброс при минимуме внутриклассового: J(w)=wᵀS_B w/wᵀS_W w, решение w∝S_W⁻¹(μ₂−μ₁). При верных гауссовых предположениях LDA/QDA — оптимальный байесовский классификатор.

**Билет 40. SVM: принцип оптимальной разделяющей гиперплоскости.** _[Воронцов]_
Линейный классификатор a(x)=sign(⟨w,x⟩−w₀). Отступ M_i=(⟨w,x_i⟩−w₀)y_i. Для линейно разделимой выборки нормируем min_i M_i=1 → ширина разделяющей полосы = 2/‖w‖. Принцип максимального зазора: ½‖w‖²→min при M_i≥1 ∀i. Опорные векторы — объекты на границе полосы (M_i=1). Широкий зазор = устойчивость и хорошее обобщение.

**Билет 41. SVM: мягкий зазор и hinge loss.** _[Воронцов]_
Для неразделимой выборки: переменные ξ_i≥0, M_i≥1−ξ_i. Задача ½‖w‖²+C∑ξ_i→min. Эквивалент безусловной: ∑(1−M_i)_+ + (1/2C)‖w‖² — hinge loss + L2-регуляризатор. Параметр C — компромисс зазор/ошибки: большой C → мало ошибок, узкий зазор, риск переобучения; малый C → широкий зазор. C обратен силе регуляризации.

**Билет 42. SVM: двойственная задача и опорные векторы.** _[Воронцов]_
Лагранжиан + ККТ. Условия седловой точки: w=∑α_i y_i x_i, ∑α_i y_i=0, 0≤α_i≤C. Двойственная задача: ∑α_i−½∑∑α_iα_j y_iy_j⟨x_i,x_j⟩→max при ограничениях. Опорный вектор — α_i≠0. Типизация по ККТ: периферийные (α=0, M>1), опорные-граничные (0<α<C, M=1), опорные-нарушители (α=C, M<1). Обучение SMO (Platt). Классификатор a(x)=sign(∑α_i y_i⟨x,x_i⟩−w₀) зависит только от опорных векторов.

**Билет 43. Ядра в SVM и спрямляющие пространства.** _[Воронцов]_
Объекты входят только через ⟨x_i,x_j⟩ → замена на ядро K(x_i,x_j)=⟨φ(x_i),φ(x_j)⟩ (kernel trick, без явного φ). Условие Мерсера: K симметрична и положительно полуопределена. Ядра: линейное, полиномиальное (⟨x,x′⟩+1)^d, RBF exp(−‖x−x′‖²/2σ²) (бесконечномерное H), сигмоидное. SVM с RBF ≈ двухслойная RBF-сеть (нейронов = опорных векторов). Параметры (C,σ,d) подбирают по CV. `[Николенко]` общий взгляд: ядро = функция похожести = скалярное произведение в пространстве признаков (RKHS).

**Билет 44. Метод k ближайших соседей (kNN).** _[Николенко + ИТМО]_
Непараметрический, ленивое обучение (нет фазы тренировки). Классификация — голосование k соседей (нечётное k, веса ∝1/d); регрессия — усреднение. Метрики: евклидова, косинусная, Хэмминга. КРИТИЧНО масштабировать признаки. Проклятие размерности: в высоких d «соседи» далеки, метод деградирует. Ускорение: k-d деревья. Сложность классификации O(n·d) на объект (дорого по памяти/времени).

**Билет 45. Наивный байесовский классификатор.** _[Николенко]_
Порождающая модель с «наивным» предположением условной независимости признаков: p(y,x)=p(y)∏ᵢp(xᵢ|y). Параметры оцениваются по частотам + сглаживание Лапласа θ̂=(#+1)/(#+M) (против нулевых вероятностей). Классификация argmax по классам. Применение — тексты (bag of words, спам). Пара с логистической регрессией: то же семейство p(y|x), но порождающее vs дискриминирующее обучение. Быстр, работает на малых данных, но предположение независимости часто нарушается.

**Билет 46. Многоклассовая классификация: OvA, OvO, softmax.** _[ИТМО, лек.4 + Николенко]_
One-vs-All: K классификаторов (k-й отличает класс k от остальных). One-vs-One: K(K−1)/2 попарных. Оба дают неоднозначные зоны. Softmax: P_j=exp(c_j(x))/∑_y exp(c_y(x)) — нормирует в распределение вероятностей, выделяет уверенный класс; обучается кросс-энтропией. Многоклассовые метрики: micro- (по суммарным TP/FP) и macro- (среднее по классам) усреднение precision/recall/F.

## Раздел 6. Метрики классификации

**Билет 47. Матрица ошибок: Accuracy, Precision, Recall.** _[ИТМО, лек.4 + Николенко]_
TP/FP/FN/TN; FP — ошибка I рода (ложная тревога), FN — ошибка II рода (пропуск). Accuracy=(TP+TN)/всего — плоха при дисбалансе (99% одного класса → 99% «даром»). Precision=TP/(TP+FP) — насколько не врёт положительный прогноз. Recall(=Sensitivity)=TP/(TP+FN) — какую долю положительных нашли. Аналогия: точность — металлоискатель не пищит на мусор; полнота — прочесал весь пляж. Антиспам → важна precision; медицина → важна recall (пропуск больного дороже).

**Билет 48. F-мера и баланс точности/полноты.** _[ИТМО, лек.4]_ (требует: Билет 47)
F₁=2·P·R/(P+R) — среднее ГАРМОНИЧЕСКОЕ (а не арифметическое: штрафует перекос, при P=1,R=0 даёт 0). Обобщённая F_β=(1+β²)PR/(β²P+R): β>1 — приоритет Recall, β<1 — Precision. Порог классификации подбирают по precision-recall trade-off под стоимость ошибок.

**Билет 49. ROC-кривая, AUC и PR-кривая.** _[собес]_ (требует: Билет 47)
Классификатор выдаёт скор; порог задаёт баланс. ROC: TPR(=Recall) vs FPR(=FP/N) при варьировании порога. ROC-AUC = площадь = вероятность, что случайный положительный получит больший скор, чем отрицательный; 0.5 случайно, 1 идеально; инвариантна к дисбалансу базовых долей. PR-кривая (Precision vs Recall) — информативнее при сильном дисбалансе (ROC-AUC оптимистично обманывает). Log-loss штрафует уверенные ошибки.

**Билет 50. Дисбаланс классов: методы борьбы.** _[собес]_
Редкий класс обычно и есть цель (мошенничество, болезни, аномалии). Методы: undersampling (проредить мажоритарный), oversampling (размножить миноритарный, риск переобучения), SMOTE (синтетические точки = выпуклые комбинации с соседями), взвешивание классов в функции потерь, подбор порога. Метрики при дисбалансе: PR-AUC, F₁, а не Accuracy/ROC-AUC. Порождающие модели (LDA/QDA) страдают меньше (баланс в p(Cₖ)).

**Билет 51. Калибровка классификаторов.** _[собес]_
`[доп.]` Хорошо откалиброванный: среди объектов с предсказанием p=0.2 доля положительных ≈20%. Недо/переуверенность. Оценка: reliability diagram, Brier score, ECE (expected calibration error). Методы калибровки: Platt scaling (сигмоида над скором), изотоническая регрессия, temperature scaling. Логрег калибрована из коробки; SVM/бустинг/деревья — часто нет.

## Раздел 7. Деревья и ансамбли

**Билет 52. Решающие деревья: ID3 и информативность.** _[Воронцов]_
Жадный синтез «разделяй и властвуй» (дерево минимальной сложности — NP-полная задача). LearnID3(U): если U чистая — лист; иначе выбрать предикат β (обычно порог [f_j≷d_j]) с максимальной информативностью, разбить U=U₀∪U₁, рекурсия. Информативность = прирост чистоты: информационный выигрыш IGain=Ĥ(родителя)−взвешенная Ĥ(потомков), где Ĥ — энтропия. В листе — доли классов как вероятности. O(B·h·n). Плюсы: интерпретируемость (путь=правило), скорость. Минусы: жадность, переобучение, чувствительность к выборке.

**Билет 53. Критерии ветвления и редукция деревьев.** _[Воронцов]_ (требует: Билет 52)
Критерии: энтропийный (information gain), индекс Джини Σp_k(1−p_k) (CART, быстрее лога), D-критерий; для регрессии — уменьшение дисперсии в листьях. Редукция против переобучения: предредукция (pre-pruning) — ранний останов при информативности <порога (жадно-неоптимальна); постредукция (post-pruning) — полное дерево, затем замена вершин листом по контрольной выборке (~30%), если уменьшает ошибки (эффективнее). Пропуски: на классификации — пропорциональное распределение по ветвям.

**Билет 54. Композиции алгоритмов и голосование.** _[Воронцов]_
Композиция a(x)=C(F(b₁(x),…,b_T(x))): базовые операторы b_t, корректирующая операция F, решающее правило C. Простое и взвешенное голосование ∑α_t b_t (выпуклое при α_t≥0,∑α_t=1). Условие пользы: базовые (а) лучше случайного, (б) РАЗНЫЕ (иначе складывать одинаковое бессмысленно). Различность обеспечивают: перевзвешивание объектов (бустинг), случайные подвыборки/подпространства (бэггинг/RSM), разные инициализации.

**Билет 55. Бэггинг и случайный лес (Random Forest).** _[Воронцов]_ (требует: Билет 21)
Бэггинг (bootstrap aggregating, Брейман): T бутстреп-подвыборок (выбор с возвращением, ~63.2% уникальных), базовые обучаются НЕЗАВИСИМО, простое голосование. Снижает ДИСПЕРСИЮ усреднением; смещение почти не меняет → нужен для моделей с низким bias/высокой variance (глубокие деревья). Out-of-bag оценка. RSM — случайные подмножества признаков. Random Forest = бэггинг глубоких деревьев + случайный выбор √n признаков в каждой вершине (декорреляция). Распараллеливается. `[собес]` feature importance (по gain / permutation).

**Билет 56. Бустинг: AdaBoost.** _[Воронцов]_ (требует: Билет 54)
Последовательное взвешенное голосование a(x)=sign∑α_t b_t(x), фокус на трудных объектах. Пороговую ошибку аппроксимируют экспонентой e^{−M}; минимизируется Q_T=∑exp(−y_i∑α_t b_t(x_i)). Теорема: оптимум при α_T=½ln((1−N)/N), N — взвешенная доля ошибок. Алгоритм: w_i=1/n; обучить b_t на взвешенной выборке, α_t, пересчёт w_i·exp(−α_t y_i b_t(x_i)), нормировка. Трудные объекты копят вес. Снижает СМЕЩЕНИЕ. Высокая обобщающая способность (качество на тесте растёт даже после нулевой ошибки на train).

**Билет 57. Градиентный бустинг.** _[Воронцов]_ (требует: Билет 56)
Обобщение на любую дифференцируемую потерю L (Фридман). Ансамбль a_T=∑γ_t b_t как градиентный спуск В ПРОСТРАНСТВЕ ФУНКЦИЙ: считаем антиградиент (псевдоостатки) −g_i=−∂L(a_{T−1,i},y_i)/∂a; базовый b_T приближает его по МНК b_T=argmin∑(b(x_i)+g_i)²; шаг γ_T=argmin∑L(a_{T−1,i}+γb_T(x_i),y_i). Для MSE антиградиент = обычные остатки. SGB — подвыборка на шаге (как бэггинг). Shrinkage ν<1 (темп обучения). GBDT над деревьями. Гиперпараметры: число деревьев, глубина, learning rate, subsample.

**Билет 58. XGBoost, LightGBM, CatBoost.** _[собес]_ (требует: Билет 57)
Промышленные GBDT — стандарт для табличных данных. XGBoost: Тейлор до 2-го порядка (градиент+гессиан), регуляризация сложности дерева (γ·#листьев+½λ∑w²), оптимальные веса листьев, гистограммные сплиты, обработка пропусков, subsample/colsample. LightGBM: leaf-wise рост, GOSS, EFB — быстрее на больших данных. CatBoost: упорядоченный бустинг (против target leakage), нативные категории (ordered target statistics), oblivious-деревья. `[собес]` частый вопрос — отличия от Random Forest (последовательный vs параллельный, bias vs variance).

**Билет 59. Стэкинг и блендинг.** _[Воронцов]_
Комбинировать базовые модели обучаемым МЕТА-алгоритмом, а не фиксированным голосованием. Стэкинг: мета-модель учится на предсказаниях базовых, полученных OUT-OF-FOLD (иначе утечка). Блендинг — то же на отложенной выборке (проще, теряет данные). Смеси экспертов (mixture of experts) с gating — вес базовой модели зависит от объекта. Отличие от голосования: мета-модель учится, КОГДА какой базовой доверять.

## Раздел 8. Кластеризация

**Билет 60. Постановка задачи кластеризации и функционалы качества.** _[ИТМО, лек.6 + Воронцов]_
Вход: объекты + функция расстояния ρ, ответов НЕТ. Выход: номер кластера a_i∈{1..K}. Цель: внутри кластера объекты похожи, между — различны. Задача НЕКОРРЕКТНА (нет единственно верного разбиения — зависит от ρ и алгоритма). Функционалы: внутрикластерное расстояние F₁→min, межкластерное F₂→max, отношение F₁/F₂→min. Проверка осмысленности: обучить классификатор на разметке-кластерах — если легко разделяет, кластеры логичны.

**Билет 61. Метод k-средних (k-means).** _[Воронцов + ИТМО, лек.6]_
Минимизировать сумму внутрикластерных квадратов ∑‖x_i−μ_{a_i}‖²→min (инерция). Алгоритм Ллойда: (1) отнести x_i к ближайшему центроиду; (2) пересчитать центроиды как средние; повторять до стабилизации. k-means = ЖЁСТКОЕ упрощение EM для GMM (сферические равные Σ). Проблемы: зависимость от инициализации (лечит k-means++), нужно задавать K, только выпуклые/сферические кластеры сопоставимого размера, локальный минимум.

**Билет 62. Выбор числа кластеров: инерция и метод локтя.** _[ИТМО, лек.6]_ (требует: Билет 61)
Инерция I(K) = сумма квадратов расстояний до центров кластеров. Свойство: монотонно убывает с ростом K (при K=n равна 0), поэтому просто «минимум» не годится. Метод локтя (elbow): график I(K); ищем точку перелома, где резкое падение сменяется плато — это K*. Ограничение: локоть часто размыт/неоднозначен.

**Билет 63. Метрика силуэта (Silhouette).** _[ИТМО, лек.6]_
Для объекта: r_i — среднее расстояние ДО СВОЕГО кластера, R_i — до ближайшего ЧУЖОГО. Силуэт S_i=(R_i−r_i)/max(R_i,r_i)∈[−1,1]. S≈1 — идеально (плотно в своём, далеко от чужого); S≈0 — на границе; S<0 — вероятно, отнесён не туда. Средний силуэт по выборке — критерий выбора K. Может противоречить методу локтя.

**Билет 64. Алгоритм DBSCAN.** _[Воронцов + ИТМО, лек.6]_
Плотностная кластеризация. Параметры: радиус ε, порог m (minPts). Типы точек: корневые (|ε-окрестность|≥m), граничные (в окрестности корневой), шумовые (выбросы). Кластер «расползается» от корневой по плотности до зон низкой плотности. Плюсы: кластеры произвольной формы (без центров), число K автоматически, детекция выбросов, O(n log n). Минусы: чувствительность к ε,m; плохо при разной плотности кластеров. `[собес]` HDBSCAN.

**Билет 65. Иерархическая (агломеративная) кластеризация.** _[Воронцов + ИТМО, лек.6]_
Снизу вверх: начать с n одноэлементных кластеров, итеративно сливать два ближайших, пересчитывать расстояния по формуле Ланса–Уильямса. Связи (linkage): single (ближнего соседа, «цепочки»), complete (дальнего, компактные), average, Ward (минимум прироста дисперсии). Результат — дендрограмма; число кластеров = срез на высоте. Плюс: не нужно задавать K заранее, видна вся иерархия. Минус: O(n²–n³).

**Билет 66. Смеси гауссиан (GMM) и EM-алгоритм.** _[Воронцов]_
GMM: p(x)=∑w_j·N(μ_j,Σ_j), w_j≥0,∑w_j=1; скрытая переменная — номер компоненты. Максимум log-правдоподобия ∑ln∑w_jN(...) не берётся аналитически → EM. E-шаг: g_ij=P(компонента j|x_i)=w_jN(x_i)/∑_s w_sN(x_i) (Байес, мягкое отнесение). M-шаг: μ_j,Σ_j как g-взвешенные среднее/ковариация, w_j=(1/n)∑g_ij. Свойства: монотонный рост правдоподобия, локальный максимум (важна инициализация). GMM = «мягкий» k-means (эллиптические кластеры, вероятностная принадлежность).

**Билет 67. Semi-supervised learning и трансдуктивный SVM (TSVM).** _[ИТМО, лек.6]_
Частичное обучение: часть данных размечена (i=1..p), часть нет (i=p+1..n). Гипотеза низкой плотности границы: граница классов должна проходить через «пустоты». TSVM штрафует и за ошибки на размеченных, и за попадание НЕРАЗМЕЧЕННЫХ в разделяющую полосу: L=τ‖w‖²+C∑_{i=1}^p(1−M_i)_+ + γ∑_{i=p+1}^n(M_i^u)_+. Предположения SSL: кластерное, многообразия, гладкости. Другие методы: self-training, label propagation. Трансдуктивное (только для данных) vs индуктивное (общий классификатор).

## Раздел 9. Сквозные вопросы

**Билет 68. Подбор гиперпараметров.** _[ИТМО, лек.6 + собес]_
Grid Search — полный перебор по сетке, экспоненциально дорог. Random Search — случайная выборка комбинаций, часто эффективнее сетки (важные параметры покрываются плотнее). Байесовская оптимизация — суррогатная модель качества + баланс exploration/exploitation (инструмент Optuna). Гиперпараметры из курса: K в k-means, C и γ в SVM, τ/λ в Ridge/Lasso, глубина/число деревьев/learning rate в бустинге. Подбор — ТОЛЬКО на валидации (nested CV), не на тесте.

**Билет 69. SVD в машинном обучении: единый взгляд.** _[ИТМО]_ (требует: Билет 6)
Три применения из курса: (1) устойчивое решение МНК через псевдообратную (отбрасывание малых σ); (2) PCA = низкоранговое приближение (Эккарт–Янг), главные компоненты = правые сингулярные векторы; (3) анализ обусловленности cond=σ_max/σ_min. Объединяет: SVD выделяет «главные направления» данных и отделяет сигнал (большие σ) от шума (малые σ).

**Билет 70. Выбор функции потерь и регуляризации под задачу.** _[ИТМО]_
Регрессия: MSE (гладко, но чувствительна к выбросам), MAE (робастна, недифференцируема в 0), Huber (компромисс). Классификация: log-loss (логрег, вероятности) vs hinge (SVM, зазор); пороговая не используется (нулевой градиент). Регуляризация как универсальный приём (τI/λI в четырёх местах): Ridge, Левенберг–Марквардт, улучшение обусловленности матрицы Грама, увеличение μ (ускорение сходимости GD). Выбор loss определяет свойства модели (робастность, разреженность, калиброванность).

## Раздел 10. Сравнение и выбор моделей

**Билет 71. Две модели с одинаковым скором — как выбрать.** _[собес-DS]_
Одна агрегированная метрика не отражает всё. Смотреть: (1) другие метрики под задачу — precision/recall/F, ROC-AUC vs PR-AUC (дисбаланс), log-loss (калибровка); (2) стабильность — std метрики по CV-фолдам и доверительный интервал; (3) статистическая значимость разницы (см. след. билет); (4) error analysis — на каких классах/сегментах ошибается каждая, коррелируют ли ошибки; (5) калибровка вероятностей; (6) робастность к сдвигу данных/выбросам; (7) прод-факторы — латентность, размер, интерпретируемость, стоимость инференса, простота поддержки, требования к данным. «Одинаковый скор» почти никогда не значит «модели эквивалентны».

**Билет 72. Статистическая значимость разницы моделей.** _[собес-DS]_ (требует: Билет 71)
Разница метрик может быть шумом. Тесты: paired t-test на метриках по k CV-фолдам (осторожно — фолды зависимы, оптимистичен); поправленный 5×2cv t-test (Дитерих) надёжнее; McNemar для двух классификаторов на ОДНОМ тесте (по несовпадениям b,c: χ²=(|b−c|−1)²/(b+c)); DeLong для сравнения двух ROC-AUC; bootstrap — доверительный интервал разницы метрик (перевыборка теста, percentile CI). Правило: если ДИ разницы включает 0 — модели значимо не различаются.

**Билет 73. Доверительные интервалы и бутстреп для метрик.** _[собес-DS]_
Точечная метрика (AUC=0.82) без ДИ малоинформативна. Бутстреп: B раз перевыбираем тест с возвращением, считаем метрику, берём percentile-интервал [2.5%,97.5%]. Аналитические ДИ: для доли (accuracy) — Wilson/Wald; для AUC — DeLong. Ширина ДИ ∝ 1/√n. Нужен, чтобы ответить «есть ли реальная разница» и корректно отчитаться стейкхолдерам (не «0.82», а «0.82 ± 0.03»).

**Билет 74. Диагностика: learning curves и validation curves.** _[собес-DS]_ (требует: Билет 21)
Learning curve — качество train/val vs размер выборки: большой разрыв train≫val при высокой val-ошибке = переобучение (high variance) → больше данных / регуляризация / проще модель; обе кривые сошлись высоко (плохо и на train) = недообучение (high bias) → сложнее модель / новые признаки. Validation curve — качество vs гиперпараметр (глубина, C, τ): видна зона недо/переобучения и оптимум. Строить ДО тюнинга — экономит время.

**Билет 75. Отбор признаков.** _[собес-DS]_
Filter (независимо от модели): корреляция/mutual information с таргетом, χ², дисперсия, VIF против мультиколлинеарности. Wrapper (перебор с моделью): forward/backward selection, RFE (recursive feature elimination) — точнее, но дорого. Embedded (встроен в обучение): L1/Lasso (зануление), важности деревьев (gain), SelectFromModel. Permutation importance — модель-агностик (перемешать признак → падение качества). Зачем: скорость, борьба с переобучением/шумом, интерпретируемость, стоимость сбора признаков.

## Раздел 11. A/B-тестирование

**Билет 76. Дизайн A/B-теста.** _[собес-DS]_
Гипотеза (H₀: эффекта нет, H₁: есть). OEC (Overall Evaluation Criterion) — главная метрика (конверсия, выручка/юзер); guardrail-метрики, которые не должны просесть (латентность, retention, отписки). Единица рандомизации — обычно ЮЗЕР, а не сессия (иначе зависимость/утечка). Случайное разбиение контроль/тест; A/A-тест для проверки корректности сплита. Метрика должна быть чувствительной, связанной с целью и быстро измеримой (proxy для долгосрочных эффектов).

**Билет 77. Размер выборки, мощность и MDE.** _[собес-DS]_
Мощность (power)=1−β — вероятность обнаружить эффект, если он есть (обычно 80%). MDE (minimum detectable effect) — минимальный эффект, который хотим ловить. Размер выборки n ∝ σ²/MDE² (растёт при меньшем MDE, большей дисперсии, большей мощности, меньшем α). Для доли грубо n≈16·p(1−p)/MDE² (α=0.05, power=0.8). Считать ДО запуска: недонабранный тест (underpowered) не поймает реальный эффект → ложный вывод «эффекта нет».

**Билет 78. Проверка значимости в A/B.** _[собес-DS]_
Метрика-доля (конверсия): z-тест двух пропорций. Средние (выручка/юзер): t-тест Уэлча (неравные дисперсии). p-value<α (0.05) → отвергаем H₀. Практичнее — доверительный интервал разницы: не пересекает 0 = значимо, и сразу видна величина эффекта. Тяжёлые хвосты/выбросы (выручка) → бутстреп или Манн-Уитни. Различать статистическую и ПРАКТИЧЕСКУЮ значимость (значимый, но крошечный эффект может не окупать внедрение).

**Билет 79. Ошибки I/II рода и множественные сравнения.** _[собес-DS]_
Ошибка I рода (α, false positive) — «нашли» эффект, которого нет; II рода (β) — пропустили реальный. При многих метриках/вариантах/срезах растёт вероятность ложного открытия (20 тестов при α=0.05 → в среднем 1 ложный). Поправки: Бонферрони (α/m, консервативно), Холм; FDR/Бенджамини-Хохберг (контролирует долю ложных среди значимых — мягче, для многих тестов). Заранее фиксировать ОДНУ основную метрику, срезы — как разведку, не как решение.

**Билет 80. Проблема подглядывания и последовательное тестирование.** _[собес-DS]_ (требует: Билет 79)
Peeking — подсматривать результат и останавливать тест при первом p<0.05 — раздувает ошибку I рода (при многократных проверках «значимость» найдётся почти наверняка). Решения: фиксировать размер/длительность заранее и смотреть один раз; group sequential с alpha-spending (О’Брайен-Флеминг); always-valid p-values / доверительные последовательности (mSPRT), байесовские методы, допускающие ранний стоп. Правило: не выключать тест по первому «зелёному».

**Билет 81. Ускорение A/B и типичные ловушки.** _[собес-DS]_
Снижение дисперсии → меньше нужный n: CUPED (вычесть предэкспериментальную ковариату, напр. прошлую активность юзера), стратификация, менее шумные метрики. Ratio-метрики (CTR=клики/показы), где единица рандомизации ≠ единица метрики → дельта-метод для корректной дисперсии (или бутстреп по юзерам). Ловушки: novelty effect (эффект новизны спадает), network effects (нарушение SUTVA — контроль и тест влияют друг на друга в соцсетях/маркетплейсах → кластерная/гео-рандомизация), парадокс Симпсона (агрегат врёт при неоднородных группах), selection/survivorship bias.

## Раздел 12. Статистические тесты (шпаргалка)

**Билет 82. Параметрические тесты.** _[собес-DS]_
t-тест: одновыборочный (среднее vs константа), двухвыборочный Стьюдента (равные дисперсии) / Уэлча (неравные), парный (связанные измерения до/после). ANOVA — сравнение средних >2 групп (F-статистика). z-тест — большие выборки / известная дисперсия / пропорции. Предположения: нормальность (или большая n по ЦПТ), независимость наблюдений, гомогенность дисперсий. При выполнении предположений мощнее непараметрических.

**Билет 83. Непараметрические тесты.** _[собес-DS]_
Не требуют нормальности (работают с рангами). Манн-Уитни U (= Уилкоксона ранговых сумм) — аналог двухвыборочного t (2 независимые группы). Уилкоксон знаково-ранговый — аналог парного t. Краскел-Уоллис — аналог ANOVA (>2 групп). Хи-квадрат — независимость категориальных признаков (таблицы сопряжённости) / согласие. KS-тест (Колмогорова-Смирнова) — совпадение распределений / сравнение с эталоном. Робастны к выбросам и ненормальности, но слабее по мощности на нормальных данных.

**Билет 84. Корреляция, нормальность и выбор теста.** _[собес-DS]_
Корреляция: Пирсон (линейная связь, чувствителен к выбросам, для инференса нужна нормальность) vs Спирмен (ранговая, монотонная, робастен) vs Кендалл. Проверка нормальности: Шапиро-Уилк (тест), QQ-plot (визуально), гистограмма. Флоучарт выбора теста: тип данных (числовые/категориальные) → число групп (1/2/>2) → парные/независимые → нормальность → параметрический / непараметрический. Всегда смотреть на график данных, а не только на p-value.

## Раздел 13. Прод и прикладное

**Билет 85. Интерпретируемость моделей.** _[собес-DS]_
Глобальная (как модель работает в целом) vs локальная (почему такой прогноз для объекта). Методы: feature importance деревьев (gain/split — смещена к признакам высокой кардинальности); permutation importance (падение качества при перемешивании — модель-агностик); SHAP (значения Шепли из теории игр — аддитивный вклад признака, локально и глобально, теоретически обоснован) — де-факто стандарт; LIME (локальная линейная аппроксимация вокруг объекта); PDP/ICE (partial dependence — влияние признака на прогноз в среднем). Trade-off точность vs интерпретируемость (линейные/деревья vs бустинг/сети).

**Билет 86. Пропуски и выбросы.** _[собес-DS]_
Пропуски: типы MCAR/MAR/MNAR. Стратегии: удаление строк/столбцов (если мало); импутация — среднее/медиана/мода (искажает дисперсию), по модели (kNN, MICE — итеративная), константа + индикатор пропуска (деревья/бустинг обрабатывают нативно). Статистики импутации считать на TRAIN (внутри CV — иначе утечка). Выбросы: детекция — z-score/IQR (за 1.5·IQR), Isolation Forest, LOF, шум DBSCAN; обработка — удалить (осторожно, могут быть сигналом), клиппинг/winsorize, робастные потери (Huber). Всегда понимать природу выброса (ошибка измерения vs редкое событие).

**Билет 87. Data drift, concept drift и мониторинг.** _[собес-DS]_
Модель деградирует со временем. Виды: data drift / covariate shift (меняется p(x)), concept drift (меняется p(y|x) — сама зависимость), label shift (p(y)). Детекция: сравнение распределений train vs prod (KS-тест, PSI — population stability index, KL-дивергенция) по признакам и предсказаниям; мониторинг качества (если приходят отложенные метки) и бизнес-метрики. Реакция: алерты, переобучение (по расписанию или по триггеру дрифта), онлайн-обучение. Мониторить одновременно данные, предсказания, качество и бизнес-метрику.

**Билет 88. Утечки данных и train-serve skew.** _[собес-DS]_
Data leakage — в обучении использована информация, недоступная на инференсе → завышенная валидация, провал в проде. Виды: target leakage (признак — следствие таргета), временная (будущее→прошлое), препроцессинг на всех данных (scaler/импутер фитить на train внутри CV), дубликаты/группы между train и test. Train-serve skew — расхождение препроцессинга/признаков между обучением и продом (разный код/источники). Лечение: единые пайплайны (sklearn Pipeline), feature store, один код фичей train=serve, временная валидация.

**Билет 89. Рекомендательные системы.** _[собес-DS]_ (требует: Билет 6)
Задача: предсказать релевантность item для user. Подходы: content-based (по признакам объектов и профилю юзера); collaborative filtering по матрице взаимодействий user×item — memory-based (user-user / item-item kNN по похожести) и model-based — матричная факторизация (SVD/ALS: R≈UVᵀ, латентные факторы). Проблема холодного старта (новый user/item без истории). Implicit feedback (клики/просмотры) vs явные оценки. Гибридные системы. Метрики — ранжирования (см. след. билет). Связь с SVD (билет 6).

**Билет 90. Метрики ранжирования.** _[собес-DS]_
Для задач с важным порядком (поиск, реко). Precision@k / Recall@k — доля релевантных в топ-k. MAP (mean average precision) — усреднённая точность по позициям релевантных. MRR (mean reciprocal rank) — 1/позиция первого релевантного. NDCG (normalized discounted cumulative gain) — учитывает градуированную релевантность И позицию (логарифмическое дисконтирование), нормирован на идеальный порядок — стандарт для поиска/реко. Также: hit rate, coverage, diversity, novelty.

**Билет 91. Причинность vs корреляция.** _[собес-DS]_
Корреляция ≠ причинность. Конфаундер — общая причина X и Y, создающая ложную связь; также selection bias, обратная причинность. Золотой стандарт причинности — рандомизированный эксперимент (A/B). Наблюдательные методы: контроль конфаундеров (стратификация, регрессия), matching / propensity score, difference-in-differences, instrumental variables, causal DAG. Парадокс Симпсона — иллюстрация опасности агрегирования. Веса регрессии — это корреляция, а не причинный эффект (нельзя интерпретировать как «увеличим X → вырастет Y»).
## Раздел 14. Временные ряды (курс ИТМО, лек.8 — для экзамена; на собесах по classic ML спрашивают реже)

**Билет 92. Временной ряд: определение и стратегии прогнозирования.** _[ИТМО, лек.8]_
Ряд {y₁,…,y_T} — значения, упорядоченные во времени; текущее зависит от прошлых (в отличие от обычной выборки). Прогнозирование сводят к регрессии на лаговых признаках (y_{t−1},…,y_{t−p}) + статистики (скользящее среднее/медиана). Стратегии: рекурсивная (одна модель, прогноз→вход для следующего шага, ошибки НАКАПЛИВАЮТСЯ); прямая (своя модель на каждый горизонт, много моделей); гибридная (прогноз как новый признак).

**Билет 93. Кросс-валидация для временных рядов.** _[ИТМО, лек.8]_
Случайное разбиение НЕДОПУСТИМО — утечка из будущего. Expanding window (расширяющееся окно: train растёт, всегда прошлое→будущее) и sliding window (скользящее окно фиксированной длины). Всегда обучаться только на данных из прошлого относительно теста.

**Билет 94. Декомпозиция временного ряда.** _[ИТМО, лек.8]_
Компоненты: тренд T_t (долгосрочная тенденция), сезонность S_t (циклы фиксированного периода), остаток R_t (шум). Аддитивная y_t=T_t+S_t+R_t (амплитуда колебаний постоянна); мультипликативная y_t=T_t·S_t·R_t (амплитуда растёт с уровнем) — сводится к аддитивной логарифмированием. STL-разложение (Seasonal-Trend using LOESS): итеративное на локальной регрессии, ловит нелинейный тренд и меняющуюся сезонность.

**Билет 95. Автокорреляция и стационарность.** _[ИТМО, лек.8]_
ACF (автокорреляционная функция) — зависимость y_t от y_{t−τ}; по коррелограмме находят период сезонности. `[доп.]` PACF (частичная) — очищена от промежуточных лагов. Стационарность (в широком смысле): E y_t=const, D y_t=const, ковариация зависит только от лага. Нужна большинству классических моделей. Проверка: тест Дики–Фуллера (ADF, H₀: нестационарен/есть единичный корень) и KPSS (H₀: стационарен) — смотрят вместе.

**Билет 96. Приведение ряда к стационарности.** _[ИТМО, лек.8]_ (требует: Билет 95)
Преобразование Бокса–Кокса (стабилизация дисперсии; логарифмирование — частный случай). Дифференцирование y′_t=y_t−y_{t−1} (снятие тренда), при необходимости повторное. Сезонное дифференцирование y′_t=y_t−y_{t−s} (снятие сезонности периода s). Порядок: сначала стабилизация дисперсии, потом дифференцирование; для прогнозов — обратное преобразование.

**Билет 97. Экспоненциальное сглаживание: SES, Хольт, Хольт–Уинтерс.** _[ИТМО, лек.8]_
SES (простое): ŷ_{t+1|t}=α y_t+(1−α)ŷ_{t|t−1} — веса прошлого убывают экспоненциально, α — скорость забывания; ловит только уровень. Модель Хольта: + уравнение тренда b_t (уровень a_t + тренд). Хольт–Уинтерс: + сезонность, параметры (α,β,γ) для уровня/тренда/сезонности; аддитивная (стабильные колебания) или мультипликативная (размах растёт с уровнем) сезонность.

**Билет 98. Модели ARIMA/SARIMA.** _[собес + ИТМО, лек.8]_ (требует: Билет 95)
`[доп.]` AR(p) — авторегрессия на p собственных лагов; MA(q) — скользящее среднее q прошлых ошибок; ARIMA(p,d,q) — d = порядок дифференцирования для стационарности. Порядки p,q подбирают по ACF/PACF (или AIC/BIC). SARIMA — + сезонные компоненты (P,D,Q,s). Тесно связано с требованием стационарности (обеспечивается частью «I»).

**Билет 99. Метрики качества прогнозов временных рядов.** _[ИТМО, лек.8 + собес]_
MAE, RMSE (в единицах ряда), MAPE (в %, ломается при y≈0), sMAPE (симметричная). MASE — нормировка на наивный прогноз (ŷ_{t+1}=y_t) как baseline. Всегда оценивать на отложенном БУДУЩЕМ отрезке, сравнивать с наивным baseline.

